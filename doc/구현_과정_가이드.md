# BlogAutoFriends 구현 과정 가이드
## Phase 1~5 전 과정 — Claude Code 작업 로그

**작성일:** 2026-04-10
**작업 환경:** Windows 11, Bash (Git Bash), Supabase CLI v2.84.2, Claude Code
**운영 프로젝트:** egwmkpplnzypkbedasrs

---

## 1. 전체 시스템 아키텍처

```
[사용자 브라우저]
    │ https://blog.pluscoach.co.kr
    ▼
[GitHub Pages]
    │ index.html + assets/css/main.css + assets/js/{config,main,form}.js
    │
    │ Supabase JS CDN (@supabase/supabase-js@2)
    ▼
[Supabase (egwmkpplnzypkbedasrs)]
    │
    ├── create_order RPC (anon 호출)
    │       ↓ INSERT 발생
    ├── orders 테이블
    │       ↓ AFTER INSERT Trigger
    ├── notify_new_order() 트리거 함수
    │       ↓ net.http_post (Authorization: Bearer + Vault service_role_key)
    │
    ▼
[Edge Function: on-new-order]
    ├─→ 카카오톡 "나에게 보내기" API ─→ 사장님 카톡
    ├─→ Telegram Bot API             ─→ 사장님 텔레그램
    └─→ Resend Email API             ─→ 사용자 이메일 (noreply@blog.pluscoach.co.kr)
```

### 데이터 흐름 상세

1. 사용자가 랜딩페이지 폼에서 이름/이메일/플랜 선택 후 제출
2. `form.js`가 `ipify` API로 사용자 IP 조회 후 `create_order` RPC 호출
3. RPC가 무료체험 이메일 중복 체크 → `orders` 테이블에 INSERT
4. Database Trigger가 `notify_new_order()` 실행
5. Trigger가 Vault에서 `service_role_key` 꺼내서 Edge Function HTTP 호출
6. Edge Function이 `Promise.allSettled`로 3개 알림 병렬 발송
7. 프론트엔드에서 성공 토스트 또는 결제 모달 표시

---

## 2. 기술 스택과 선택 이유

| 계층 | 기술 | 선택 이유 |
|------|------|----------|
| **프론트엔드** | HTML + Tailwind CDN + Vanilla JS | 빌드 도구 없이 GitHub Pages 즉시 배포. MVP에 프레임워크 불필요 |
| **폰트** | Pretendard (CDN) | 한국어 최적화, Apple SD Gothic Neo 대체 |
| **백엔드** | Supabase (PostgreSQL) | 기존 프로그램 라이선스 시스템과 동일 프로젝트 사용. 무료 티어 |
| **RPC** | plpgsql SECURITY DEFINER | RLS 우회하면서 비즈니스 로직(중복체크, 코드생성) 서버사이드 처리 |
| **Trigger** | pg_net + Vault | DB INSERT 이벤트로 Edge Function 자동 호출. Vault로 키 안전 관리 |
| **Edge Function** | Deno/TypeScript | Supabase 네이티브. 콜드스타트 빠름 |
| **카카오톡 알림** | "나에게 보내기" API | 사장님 카톡에 직접 알림. 비즈니스 메시지 대비 심사 불필요 |
| **텔레그램 알림** | Bot API | 카카오톡 장애 시 백업. 설정 5분 |
| **이메일** | Resend | 개발자 친화적 API, 무료 월 3000건, 도메인 인증 간단 |
| **배포** | GitHub Pages + 카페24 CNAME | 무료. 서브도메인으로 기존 사이트 영향 0 |
| **토큰 관리** | kakao_tokens 테이블 + 자동갱신 | access_token 6시간 만료 → Edge Function에서 자동 refresh |

---

## 3. Supabase 프로젝트 구조

### 3.1 테이블

| 테이블 | 상태 | RLS | 용도 |
|--------|------|-----|------|
| `licenses` | 기존 (절대 수정 금지) | ON + 정책 있음 | 프로그램 라이선스 관리 |
| `orders` | Phase 3 신규 | ON + 정책 없음 (RPC만 접근) | 주문/신청 관리 |
| `kakao_tokens` | Phase 3 신규 | ON + 정책 없음 (service_role만) | 카카오 토큰 단일 행 |

### 3.2 RPC 함수

| 함수 | 권한 | 설명 |
|------|------|------|
| `verify_and_activate(key, machine_id)` | anon | 기존 — 프로그램 라이선스 검증 |
| `update_session(key, machine_id)` | anon | 기존 — 세션 갱신 |
| `clear_session(key)` | anon | 기존 — 세션 정리 |
| `create_order(p_name, p_email, p_plan, p_amount, p_ip, p_user_agent)` | anon | 신규 — 주문 생성 + 중복체크 |
| `create_license(p_buyer_name, p_plan, p_order_code)` | service_role 전용 | 신규 — 라이선스 키 발급 |

### 3.3 Trigger

| 트리거 | 테이블 | 이벤트 | 함수 |
|--------|--------|--------|------|
| `on_orders_updated` | orders | BEFORE UPDATE | `handle_updated_at()` — updated_at 자동 갱신 |
| `on_order_inserted` | orders | AFTER INSERT | `notify_new_order()` — Edge Function 호출 |

### 3.4 Extensions

| 확장 | 용도 |
|------|------|
| `pg_net` | Trigger에서 HTTP POST (Edge Function 호출) |
| `supabase_vault` | service_role_key 암호화 저장 |

### 3.5 Vault

```sql
-- service_role_key 저장 (한 번만 실행)
SELECT vault.create_secret('실제_SERVICE_ROLE_KEY', 'service_role_key');

-- Trigger에서 읽기
SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'service_role_key';
```

### 3.6 Edge Function

| 파일 | 줄 수 | 역할 |
|------|-------|------|
| `on-new-order/index.ts` | 74줄 | 메인 핸들러. 3중 알림 병렬 발송 |
| `_shared/kakao.ts` | 128줄 | 카카오 "나에게 보내기" + 토큰 자동갱신 |
| `_shared/telegram.ts` | 42줄 | 텔레그램 봇 발송 |
| `_shared/resend.ts` | 76줄 | Resend 이메일 (HTML 템플릿 포함) |

### 3.7 Edge Function Secrets (6개)

```
KAKAO_REST_API_KEY          # 카카오 REST API 키
TELEGRAM_BOT_TOKEN          # 텔레그램 봇 토큰
TELEGRAM_CHAT_ID            # 사장님 텔레그램 ID
RESEND_API_KEY              # Resend API 키
RESEND_FROM_EMAIL           # noreply@blog.pluscoach.co.kr
SUPABASE_DB_URL             # (미사용, 기존에 있던 것)
# SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY 는 Supabase가 자동 주입
```

---

## 4. 사전 준비물 10개 항목 상세

### Phase 2 필요 (2개)

| # | 항목 | 소요 | 상세 |
|---|------|------|------|
| 1 | **Supabase CLI 설치** | 5분 | `scoop install supabase` (Windows) 또는 `npm install -g supabase` |
| 2 | **Supabase DB 비밀번호** | 2분 | Dashboard → Settings → Database → Connection string |

### Phase 5 필요 (7개)

| # | 항목 | 소요 | 상세 |
|---|------|------|------|
| 3 | **Kakao Developers 앱 등록** | 20분 | developers.kakao.com → 앱 추가 → REST API 키 복사 |
| 4 | **Kakao 플랫폼 + 로그인 설정** | 10분 | Web 플랫폼 등록 + 카카오 로그인 ON + Redirect URI + 동의항목(talk_message) |
| 5 | **Kakao 초기 토큰 발급** | 10분 | OAuth 인증코드 → curl로 access/refresh token 발급 → kakao_tokens INSERT |
| 6 | **텔레그램 봇 생성** | 5분 | @BotFather → /newbot → 토큰 복사 |
| 7 | **텔레그램 chat_id 확인** | 3분 | @userinfobot → /start → User ID 확인 |
| 8 | **Resend 계정 + API 키** | 5분 | resend.com 가입 → API Keys → Create |
| 9 | **Resend 도메인 인증** | 15분 | Resend에서 DNS 레코드 확인 → 카페24에 MX/TXT 추가 → Verify |

### Phase 8 필요 (1개)

| # | 항목 | 소요 | 상세 |
|---|------|------|------|
| 10 | **카페24 CNAME 레코드** | 5분 | 호스트: `blog`, 값: `jsrb0.github.io` |

### Kakao 토큰 발급 curl 명령어

```bash
# Step 1: 브라우저에서 인증코드 받기 (REST_API_KEY 교체)
# https://kauth.kakao.com/oauth/authorize?client_id=[REST_API_KEY]&redirect_uri=https://localhost:3000/oauth/callback&response_type=code&scope=talk_message

# Step 2: 인증코드로 토큰 발급
curl -X POST "https://kauth.kakao.com/oauth/token" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "grant_type=authorization_code" \
  -d "client_id=[REST_API_KEY]" \
  -d "client_secret=[CLIENT_SECRET]" \
  -d "redirect_uri=https://localhost:3000/oauth/callback" \
  -d "code=[인증코드]"

# Step 3: kakao_tokens 테이블에 INSERT
INSERT INTO public.kakao_tokens (id, access_token, refresh_token, access_token_expires_at)
VALUES (1, '[access_token]', '[refresh_token]', NOW() + INTERVAL '21599 seconds')
ON CONFLICT (id) DO UPDATE SET
  access_token = EXCLUDED.access_token,
  refresh_token = EXCLUDED.refresh_token,
  access_token_expires_at = EXCLUDED.access_token_expires_at,
  updated_at = NOW();
```

### Resend 도메인 인증 — 카페24 DNS 레코드

```
# 카페24 DNS 관리에서 추가 (호스트 부분만 입력, pluscoach.co.kr은 자동 붙음)

① MX    | 호스트: send.blog                | 값: feedback-smtp.us-east-1.amazonses.com (priority 10)
② TXT   | 호스트: send.blog                | 값: v=spf1 include:amazonses.com ~all
③ TXT   | 호스트: resend._domainkey.blog   | 값: (Resend Dashboard에서 복사한 DKIM 문자열)
④ TXT   | 호스트: _dmarc.blog              | 값: v=DMARC1; p=none; (선택)
```

---

## 5. Phase별 진행 과정

### Phase 1: 구조 리팩터링 (2026-04-09)

**목표:** 1724줄 단일 index.html → CSS/JS 분리

**작성/수정 파일:**

| 파일 | 줄 수 | 내용 |
|------|-------|------|
| `assets/css/main.css` | 68줄 | `<style>` 블록 7개 통합 (글로벌, 토스트, 모달, 애니메이션 등) |
| `assets/js/config.js` | 19줄 | APP_CONFIG 구조체 (URL/key 비워둠) |
| `assets/js/main.js` | 167줄 | UI 로직 (스크롤, 토스트, 애니메이션, 타이핑) |
| `assets/js/form.js` | 81줄 | Supabase 초기화 + submitForm |
| `index.html` | 1407줄 | 인라인 style/script 제거, 외부 파일 링크 |

**검수:**
- 브라우저에서 디자인 100% 동일 확인
- 모든 인터랙션 (스크롤, 토스트, 모달, 타이핑) 정상 작동

---

### Phase 2: Supabase CLI 설정 (2026-04-09)

**실행한 명령어:**

```bash
# CLI 설치
scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
scoop install supabase
supabase --version  # v2.84.2

# 인증
supabase login  # 브라우저 인증

# 프로젝트 초기화 + 연결
supabase init
supabase link --project-ref egwmkpplnzypkbedasrs
```

**수정 파일:**
- `supabase/config.toml` — `[functions.on-new-order] verify_jwt = false` 추가
- `.gitignore` — `supabase/.env`, `supabase/.temp/` 추가
- 문서 4개에서 구 Ref `lsykymwqzlypnichhids` → 신 Ref `egwmkpplnzypkbedasrs` 14건 치환

---

### Phase 3: DB 스키마 (2026-04-09)

**Migration 5개 작성:**

| 파일 | 내용 |
|------|------|
| `20260409000100_orders_table.sql` | orders 테이블 + RLS + updated_at 트리거 + 인덱스 |
| `20260409000200_create_order_rpc.sql` | create_order RPC (중복체크, 코드생성, anon GRANT) |
| `20260409000300_create_license_rpc.sql` | create_license RPC (service_role 전용) |
| `20260409000400_kakao_tokens.sql` | kakao_tokens 테이블 (단일 행 제약) |
| `20260409000500_notify_trigger.sql` | pg_net + Vault + DB Trigger |

**실행한 명령어:**

```bash
supabase db push
```

**Vault 초기 설정 (SQL Editor에서 실행):**

```sql
SELECT vault.create_secret('실제_SERVICE_ROLE_KEY', 'service_role_key');
```

**검수 SQL:**

```sql
-- 테이블 확인
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public' AND table_type = 'BASE TABLE';
-- 결과: licenses, orders, kakao_tokens

-- RPC 확인
SELECT routine_name FROM information_schema.routines
WHERE routine_schema = 'public' AND routine_type = 'FUNCTION';
-- 결과: verify_and_activate, update_session, clear_session, create_order, create_license 등

-- 트리거 확인
SELECT trigger_name, event_object_table, event_manipulation
FROM information_schema.triggers WHERE trigger_schema = 'public';
-- 결과: on_orders_updated (BEFORE UPDATE), on_order_inserted (AFTER INSERT)

-- create_order 실제 호출 테스트
SELECT create_order('테스트', 'test@example.com', 'free_trial', 0, '127.0.0.1', 'test');
-- 결과: {"id": 1, "order_code": "테스트-A3X7", "plan": "free_trial", "status": "처리중"}

-- 중복 차단 테스트
SELECT create_order('테스트', 'test@example.com', 'free_trial', 0, '127.0.0.1', 'test');
-- 결과: ERROR: 이미 무료 체험을 신청한 이메일입니다.

-- 테스트 데이터 정리
DELETE FROM public.orders WHERE email = 'test@example.com';
```

---

### Phase 4: 폼 연결 (2026-04-09)

**수정 파일:**

`assets/js/config.js`:
```js
window.APP_CONFIG = {
    supabase: {
        url: 'https://egwmkpplnzypkbedasrs.supabase.co',
        anonKey: '실제_ANON_KEY'
    },
    plans: {
        free:     { plan: 'free_trial',    amount: 0 },
        '1month': { plan: 'monthly',       amount: 39000 },
        full:     { plan: 'full_package',  amount: 59000 }
    },
    kakao: { channelUrl: 'https://open.kakao.com/me/pluscoach' },
    site: { url: '' }
};
```

`assets/js/form.js` 주요 변경:
- `supabaseClient.from('orders').insert(...)` → `supabaseClient.rpc('create_order', {...})`
- `getUserIP()` 함수 추가 (ipify API)
- `navigator.userAgent` 전달
- RAISE EXCEPTION 한글 에러 → 토스트 표시

**검수:**
- 무료체험 제출 → 토스트 + orders에 `free_trial`/`처리중` INSERT 확인
- 동일 이메일 재제출 → "이미 무료 체험을 신청한 이메일입니다." 토스트 확인
- 유료 플랜 → 결제 모달 표시 확인
- orders 테이블에 name/email/plan/amount/ip/user_agent/order_code 전부 기록 확인

---

### Phase 5: Edge Function + 3중 알림 (2026-04-09 ~ 04-10)

#### Phase 5-A: 코드 작성 (04-09)

**작성 파일 4개:**

`supabase/functions/on-new-order/index.ts` (74줄):
- Trigger의 `{record: {...}}` 페이로드 수신
- `Promise.allSettled`로 kakao + telegram + resend 병렬 발송
- 항상 200 반환 (Trigger 재시도 방지)

`supabase/functions/_shared/kakao.ts` (128줄):
- `getValidAccessToken()` — kakao_tokens에서 읽기, 만료 5분 전 자동갱신
- `refreshAccessToken()` — refresh_token으로 재발급 + DB 업데이트
- `sendKakaoNotification()` — 카카오 "나에게 보내기" API

`supabase/functions/_shared/telegram.ts` (42줄):
- `sendTelegramNotification()` — Bot API sendMessage

`supabase/functions/_shared/resend.ts` (76줄):
- `sendOrderConfirmationEmail()` — HTML 이메일 템플릿
- 플랜별 분기 (무료: 결제 안내 생략, 유료: 결제 안내 포함)

#### Phase 5-B: 사전 준비 + 배포 (04-10)

**사전 준비 완료 항목:**
1. 텔레그램 봇 생성 → TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID 확보
2. Kakao Developers 앱 등록 → KAKAO_REST_API_KEY 확보
3. Kakao OAuth 인증코드 발급 → curl로 access/refresh token 발급
4. kakao_tokens 테이블 INSERT 완료
5. Resend 계정 + API 키 발급
6. Resend 도메인 인증 (blog.pluscoach.co.kr) 완료
7. Edge Function Secrets 6개 등록 완료

**배포 명령어:**

```bash
supabase functions deploy on-new-order --no-verify-jwt
```

**배포 결과:**
```
Uploading asset (on-new-order): supabase/functions/on-new-order/index.ts
Uploading asset (on-new-order): supabase/functions/_shared/resend.ts
Uploading asset (on-new-order): supabase/functions/_shared/telegram.ts
Uploading asset (on-new-order): supabase/functions/_shared/kakao.ts
Deployed Functions on project egwmkpplnzypkbedasrs: on-new-order
```

**함수 URL:** `https://egwmkpplnzypkbedasrs.supabase.co/functions/v1/on-new-order`

**실전 테스트:**
- 폼 제출 → orders INSERT → Trigger 발동 → Edge Function 호출
- 카카오톡 알림 수신 확인
- 텔레그램 알림 수신 확인
- 사용자 이메일 수신 확인

---

## 6. 발생한 오류와 해결법

### 6.1 supabase link 권한 거부

**증상:** `supabase link --project-ref ...` 실행 시 권한 거부 에러

**원인:** `supabase login`에 사용한 Supabase 계정이 해당 프로젝트의 소유자/멤버가 아님. 이전 계정으로 로그인된 상태.

**해결:**
```bash
supabase logout
supabase login  # 올바른 계정으로 브라우저 인증
supabase link --project-ref egwmkpplnzypkbedasrs
```

---

### 6.2 마이그레이션 버전 충돌 (파일명 동일)

**증상:** `supabase db push` 시 5개 마이그레이션 중 첫 번째만 실행되고 나머지 무시

**원인:** 초기 파일명이 `20260409_001_xxx.sql`, `20260409_002_xxx.sql` 형태였는데, Supabase가 버전 번호를 숫자 prefix로 추출하여 5개 모두 `20260409`로 인식 → 충돌

**해결:** 파일명을 고유 타임스탬프로 변경:
```
20260409_001_orders_table.sql       → 20260409000100_orders_table.sql
20260409_002_create_order_rpc.sql   → 20260409000200_create_order_rpc.sql
20260409_003_create_license_rpc.sql → 20260409000300_create_license_rpc.sql
20260409_004_kakao_tokens.sql       → 20260409000400_kakao_tokens.sql
20260409_005_notify_trigger.sql     → 20260409000500_notify_trigger.sql
```

---

### 6.3 카카오 KOE006 (Redirect URI 미등록)

**증상:** OAuth 인증코드 요청 시 `KOE006: Redirect URI mismatch` 에러

**원인:** Kakao Developers에서 Redirect URI를 등록하지 않았거나, 등록 위치를 잘못 찾음

**해결:**
1. Kakao Developers → 내 애플리케이션 → 해당 앱 선택
2. **앱 키** 메뉴가 아니라 **카카오 로그인** 메뉴로 이동
3. **Redirect URI** 항목에 `https://localhost:3000/oauth/callback` 등록
4. **주의:** "플랫폼" 메뉴의 사이트 도메인과는 별개. Redirect URI는 카카오 로그인 설정 내에 있음

---

### 6.4 카페24 SPF 관리 메뉴가 IP만 받음

**증상:** Resend 도메인 인증용 SPF TXT 레코드(`v=spf1 include:amazonses.com ~all`)를 카페24에 등록하려 하니, "SPF 관리" 메뉴에서는 IP 주소만 입력 가능

**원인:** 카페24의 SPF 전용 UI는 IP 기반만 지원. include 구문은 TXT 레코드로 직접 등록해야 함

**해결:**
1. 카페24 DNS 관리 → "SPF 관리"가 아닌 **"TXT 레코드 관리"** 메뉴 사용
2. 호스트: `send.blog`, 값: `v=spf1 include:amazonses.com ~all` 직접 입력

---

### 6.5 Vault current_setting 권한 문제

**증상:** 초기 설계에서 `current_setting('app.settings.service_role_key')` 방식으로 service_role_key를 저장하려 했으나, Supabase 환경에서 `ALTER DATABASE SET` 권한 없음

**원인:** Supabase 매니지드 환경에서 PostgreSQL `current_setting`에 커스텀 변수를 등록하는 `ALTER DATABASE` 명령에 제한이 있음

**해결:** Supabase Vault 사용으로 전환:
```sql
-- 저장
SELECT vault.create_secret('실제_SERVICE_ROLE_KEY', 'service_role_key');

-- 읽기 (Trigger 함수 내에서)
SELECT decrypted_secret FROM vault.decrypted_secrets WHERE name = 'service_role_key';
```

**장점:** Vault가 더 안전함 (암호화 저장, 접근 제어). `current_setting`은 평문이었을 것.

---

## 7. 보안 가드레일

### 7.1 licenses 테이블 보호

```
- RLS 활성화 + 기존 정책 유지
- create_license RPC는 INSERT만 수행 (테이블 구조 변경 0)
- create_license에 anon GRANT 없음 → service_role(Edge Function)만 호출 가능
```

### 7.2 DNS 격리

```
pluscoach.co.kr          → 기존 자동매매 페이지 (A/CNAME 수정 금지)
blog.pluscoach.co.kr     → 랜딩페이지 (CNAME → jsrb0.github.io)
send.blog.pluscoach.co.kr → Resend 이메일 인증 (MX/TXT)

모든 DNS 변경은 "blog" 서브도메인 이하에서만 발생
```

### 7.3 service_role_key 관리

```
저장 위치:
1. Supabase Vault (vault.create_secret) — DB Trigger에서 사용
2. Edge Function Secrets (자동 주입: SUPABASE_SERVICE_ROLE_KEY) — Edge Function 내부

절대 금지:
- 프론트엔드 코드에 하드코딩
- Git 커밋
- config.js 등 공개 파일
```

### 7.4 RLS 설계

| 테이블 | RLS | anon 접근 | 접근 방법 |
|--------|-----|-----------|-----------|
| orders | ON, 정책 없음 | 불가 | create_order RPC (SECURITY DEFINER) |
| kakao_tokens | ON, 정책 없음 | 불가 | Edge Function (service_role) |
| licenses | ON, 정책 있음 | 기존 RPC만 | verify_and_activate 등 |

### 7.5 Edge Function 인증

```
--no-verify-jwt 플래그로 배포
→ Database Trigger에서 JWT 없이 호출 가능
→ 대신 Authorization: Bearer {service_role_key} 헤더로 인증
→ Trigger 함수가 Vault에서 키를 꺼내서 헤더에 포함
```

---

## 8. 재사용 템플릿

### 8.1 비슷한 프로젝트 시작 체크리스트

```
□ 1. Supabase CLI 설치 + login
□ 2. 프로젝트 init + link
□ 3. config.toml에서 Edge Function JWT 설정
□ 4. Migration 파일 작성 (고유 타임스탬프 필수!)
     └ 파일명: YYYYMMDDHHMMSS_description.sql
□ 5. Vault에 service_role_key 저장
□ 6. supabase db push (운영 DB 반영)
□ 7. Edge Function 코드 작성 (_shared/ 패턴)
□ 8. Edge Function Secrets 등록
□ 9. supabase functions deploy [name] --no-verify-jwt
□ 10. 실전 테스트
```

### 8.2 3중 알림 시스템 템플릿

```
Edge Function 구조:
  functions/
  ├── [trigger-name]/
  │   └── index.ts          # Promise.allSettled로 병렬 발송
  └── _shared/
      ├── kakao.ts          # 카카오 "나에게 보내기" + 토큰 자동갱신
      ├── telegram.ts       # 텔레그램 봇 메시지
      └── resend.ts         # Resend HTML 이메일

필수 Secrets:
  KAKAO_REST_API_KEY, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID,
  RESEND_API_KEY, RESEND_FROM_EMAIL

필수 테이블:
  kakao_tokens (id=1 단일 행, access/refresh token + 만료시간)
```

### 8.3 Database Trigger → Edge Function 패턴

```sql
-- 1. Extensions
CREATE EXTENSION IF NOT EXISTS pg_net WITH SCHEMA extensions;
CREATE EXTENSION IF NOT EXISTS supabase_vault WITH SCHEMA vault;

-- 2. Vault에 키 저장
SELECT vault.create_secret('[SERVICE_ROLE_KEY]', 'service_role_key');

-- 3. Trigger 함수
CREATE OR REPLACE FUNCTION public.notify_on_insert()
RETURNS TRIGGER AS $$
DECLARE v_key TEXT;
BEGIN
    SELECT decrypted_secret INTO v_key
    FROM vault.decrypted_secrets WHERE name = 'service_role_key' LIMIT 1;

    PERFORM net.http_post(
        url := 'https://[PROJECT_REF].supabase.co/functions/v1/[FUNCTION_NAME]',
        headers := jsonb_build_object(
            'Content-Type', 'application/json',
            'Authorization', 'Bearer ' || v_key
        ),
        body := jsonb_build_object('record', row_to_json(NEW)::jsonb)
    );
    RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- 4. Trigger 연결
CREATE TRIGGER on_table_inserted
    AFTER INSERT ON public.[TABLE_NAME]
    FOR EACH ROW EXECUTE FUNCTION public.notify_on_insert();
```

### 8.4 프론트엔드 → Supabase RPC 패턴

```js
// config.js
window.APP_CONFIG = {
    supabase: {
        url: 'https://[PROJECT_REF].supabase.co',
        anonKey: '[ANON_KEY]'
    }
};

// form.js
const supabaseClient = supabase.createClient(
    APP_CONFIG.supabase.url,
    APP_CONFIG.supabase.anonKey
);

const { data, error } = await supabaseClient.rpc('create_order', {
    p_name: name,
    p_email: email,
    p_plan: plan,
    p_amount: amount,
    p_ip: await getUserIP(),
    p_user_agent: navigator.userAgent
});

if (error) throw new Error(error.message);
// RAISE EXCEPTION 메시지가 error.message에 담겨옴
```

---

## 9. 파일 구조 (Phase 5 완료 시점)

```
blogautopage/
├── CLAUDE.md                                    # 리포 가드레일
├── index.html                                   # 메인 랜딩페이지 (1407줄)
├── index copy.html                              # 원본 백업
├── .gitignore
│
├── assets/
│   ├── css/main.css                             # 통합 CSS (68줄)
│   └── js/
│       ├── config.js                            # Supabase URL/key + 플랜 (19줄)
│       ├── main.js                              # UI 로직 (167줄)
│       └── form.js                              # RPC 호출 + submitForm (81줄)
│
├── supabase/
│   ├── config.toml                              # verify_jwt = false
│   ├── migrations/
│   │   ├── 20260409000100_orders_table.sql
│   │   ├── 20260409000200_create_order_rpc.sql
│   │   ├── 20260409000300_create_license_rpc.sql
│   │   ├── 20260409000400_kakao_tokens.sql
│   │   └── 20260409000500_notify_trigger.sql
│   └── functions/
│       ├── on-new-order/index.ts                # 메인 핸들러 (74줄)
│       └── _shared/
│           ├── kakao.ts                         # 카카오 알림 (128줄)
│           ├── telegram.ts                      # 텔레그램 알림 (42줄)
│           └── resend.ts                        # 이메일 알림 (76줄)
│
└── doc/
    ├── 인수인계_v3_FINAL.md                      # 마스터 설계 문서
    ├── 사전준비_체크리스트_FINAL.md                # 자격 증명 준비 가이드
    ├── Claude_Code_시작_프롬프트_FINAL.md          # 시작 프롬프트
    ├── 구현 가이드.md                             # 기술 상세 문서
    ├── 구현_과정_가이드.md                         # 이 파일
    └── 진행상황.md                                # Phase별 진행 현황
```
