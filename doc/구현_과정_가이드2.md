# BlogAutoFriends 구현 과정 가이드 (Part 2 — 현재 시스템)
## Phase 12 이후 — 운영 기준 문서

**최종 업데이트:** 2026-04-19
**작업 환경:** Windows 11, Bash (Git Bash), Supabase CLI v2.84.2, Claude Code
**운영 프로젝트:** egwmkpplnzypkbedasrs
**라이브:** https://blog.pluscoach.co.kr (HTTPS, GitHub Pages)
**최신 인수인계:** doc/인수인계_v8_광고 픽셀 셋팅.md (v8, 2026-04-19)
**과거 작업 기록:** `doc/구현_과정_가이드1.md` (Phase 1~11 아카이브)

---

## 1. 시스템 아키텍처 (현재)

```
[사용자 브라우저]
    │ https://blog.pluscoach.co.kr
    ▼
[GitHub Pages]
    │ index.html + assets/{css,js}/*
    │ PayApp JS SDK (payapp-lite.js, dormant)
    │ Supabase JS CDN (@supabase/supabase-js@2)
    ▼
[Supabase (egwmkpplnzypkbedasrs)]
    │
    ├── create_order RPC (anon 호출, 7파라미터)
    │       ↓ INSERT 발생
    ├── orders 테이블
    │       ↓ AFTER INSERT Trigger
    ├── notify_new_order() 트리거 함수
    │       ↓ net.http_post (Authorization: Bearer + Vault service_role_key)
    │
    ▼
[Edge Function: on-new-order]
    │
    ├── plan === 'free_trial' → 3중 알림 + 자동 라이선스 발급 + 인증키 이메일
    │   ├─→ 카카오톡 "나에게 보내기" ─→ 사장님 카톡
    │   ├─→ Telegram Bot API        ─→ 사장님 텔레그램
    │   ├─→ Resend 접수 확인 이메일  ─→ 사용자
    │   ├─→ create_license RPC      ─→ licenses INSERT
    │   └─→ Resend 인증키 이메일    ─→ 사용자 (DOWNLOAD_URL_FREE)
    │
    └── plan !== 'free_trial' → PAYMENT_MODE 분기
        │
        ├── 'bank_transfer' (현재 활성) ───────────────────────────
        │   ├─→ sendDepositNoticeWithButtons ─→ 사장님 텔레그램
        │   │     (인라인 버튼: [✅ 승인] [📧 리마인더])
        │   ├─→ orders.telegram_notice_message_id UPDATE
        │   │
        │   │  [프론트: 무통장 모달 표시]
        │   │       ↓ 고객이 "입금 완료" 빨간 버튼 클릭
        │   │
        │   ├─→ [notify-deposit-confirmed]
        │   │   ├─→ orders.status = '입금대기'
        │   │   └─→ sendDepositReceivedNotice (답장 연결)
        │   │
        │   │       ↓ 사장님이 [✅ 승인] 클릭
        │   │
        │   └─→ [telegram-callback]
        │       ├─→ create_license → licenses INSERT
        │       ├─→ orders UPDATE (license_key + status='결제완료')
        │       ├─→ Resend 인증키 이메일
        │       └─→ 재발송 버튼 (같은 이메일 / 다른 이메일)
        │
        └── 'payapp' (dormant) ─────────────────────────────────
            ↓ 조기 반환 → 프론트 PayApp 결제창
            ↓ 사용자 결제 완료
            ↓ [payapp-webhook] → 라이선스 발급 + 인증키 이메일
```

---

## 2. 데이터 흐름

### 무료체험
1. 사용자 폼 제출 → `create_order` RPC → INSERT (status='처리중')
2. Trigger → on-new-order → 3중 알림 + 라이선스 발급 + 인증키 이메일
3. 프론트 성공 토스트

### 유료 무통장 입금 (현재 활성)
1. 사용자 폼 제출 (이름/이메일/전화/플랜) → `create_order` RPC → INSERT (status='결제대기')
2. Trigger → on-new-order → 사장님 텔레그램 알림 (버튼 포함) + message_id 저장
3. 프론트 무통장 모달 표시 (계좌 정보 + 빨간 버튼)
4. 고객 빨간 버튼 → notify-deposit-confirmed → status='입금대기' + 답장 알림
5. 사장님 텔레그램 [✅ 승인] → telegram-callback → 라이선스 발급 + 이메일 발송

### 유료 페이앱 결제 (PAYMENT_MODE='payapp' 시)
1. 사용자 폼 제출 → `create_order` → INSERT (status='결제대기')
2. Trigger → on-new-order → 조기 반환
3. 프론트 PayApp 결제창 → 결제 완료 → payapp-webhook → 라이선스 + 이메일

---

## 3. 결제 모드 스위치

| 모드 | 프론트 (config.js) | 백엔드 (Secret) | 상태 |
|------|-------------------|----------------|------|
| **무통장** | `paymentMode: 'bank_transfer'` | `PAYMENT_MODE=bank_transfer` | **현재 활성** |
| 페이앱 | `paymentMode: 'payapp'` | `PAYMENT_MODE=payapp` | dormant |

### 페이앱 복귀 방법
```bash
# 1. 백엔드
supabase secrets set PAYMENT_MODE=payapp

# 2. 프론트 (config.js)
paymentMode: 'payapp'   # 이 한 줄만 변경

# 3. git push → GitHub Pages 자동 배포
```
무통장 코드는 dormant 유지 — 삭제 없음.

### 무통장 입금 계좌
| 항목 | 값 |
|------|-----|
| 은행 | 하나은행 |
| 계좌번호 | 120-910375-30807 |
| 예금주 | 오준석(제이에스코퍼레이션) |

---

## 4. DB 구조 (현재)

### 테이블

| 테이블 | RLS | 용도 |
|--------|-----|------|
| `licenses` | ON (기존 정책) | 프로그램 라이선스 — **절대 수정 금지** |
| `orders` | ON (정책 없음, RPC만) | 주문 관리 |
| `kakao_tokens` | ON (service_role만) | 카카오 토큰 단일 행 |

### orders 주요 컬럼

| 컬럼 | 타입 | 용도 |
|------|------|------|
| id | BIGSERIAL PK | - |
| name, email | TEXT NOT NULL | 고객 정보 |
| phone | TEXT DEFAULT '' | 전화번호 (Phase 12) |
| plan | TEXT CHECK (free_trial/monthly/full_package/lifetime) | 플랜 |
| amount | INTEGER | 금액 |
| status | TEXT DEFAULT '결제대기' | 처리중/결제대기/입금대기/결제완료/발급실패/이메일실패/데이터오류/금액불일치 |
| order_code | TEXT UNIQUE | {이름}-{랜덤4자리} |
| license_key | TEXT | 발급된 라이선스 키 |
| telegram_notice_message_id | BIGINT NULL | 텔레그램 알림 답장 연결용 (Phase 12) |
| ip, user_agent | TEXT | 접속 정보 |
| created_at, updated_at | TIMESTAMPTZ | 시각 |

### RPC 함수

| 함수 | 권한 | 파라미터 |
|------|------|---------|
| `create_order` | anon | p_name, p_email, p_plan, p_amount, p_ip, p_user_agent, **p_phone** (7개) |
| `create_license` | service_role | p_buyer_name, p_plan, p_order_code |
| `verify_and_activate` | anon | 기존 — 프로그램 라이선스 검증 |

### Trigger

| 트리거 | 이벤트 | 함수 | payload |
|--------|--------|------|---------|
| on_order_inserted | AFTER INSERT | notify_new_order() | id, name, email, **phone**, plan, amount, status, order_code, ip, user_agent, created_at |
| on_orders_updated | BEFORE UPDATE | handle_updated_at() | updated_at 자동 갱신 |

---

## 5. Edge Functions (현재)

| 함수 | 버전 | 줄 수 | 역할 |
|------|------|-------|------|
| on-new-order | v21 | ~203 | 무료체험 3중 알림 + 유료 PAYMENT_MODE 분기 |
| payapp-webhook | v10 | ~168 | 페이앱 결제 Webhook (dormant) |
| telegram-callback | v6 | ~663 | 텔레그램 버튼 callback — 승인/리마인더/재발송 |
| notify-deposit-confirmed | v2 | ~160 | 고객 입금 완료 버튼 → status 변경 + 답장 알림 |

### _shared 모듈

| 파일 | 줄 수 | export 함수 |
|------|-------|-------------|
| telegram.ts | ~474 | sendTelegramNotification, sendPaidOrderTelegram, **sendDepositNoticeWithButtons**, **editTelegramMessage**, **answerCallbackQuery**, **sendTelegramMessage**, **deleteTelegramMessage**, **sendDepositNoticeInfoOnly** (dormant), **sendDepositReceivedNotice**, escapeHtml(내부) |
| resend.ts | ~529 | sendOrderConfirmationEmail, sendLicenseKeyEmail, **sendReminderEmail**, **sendEmergencyAlertEmail**, escapeHtml(내부), getPlanLabel(내부) |
| kakao.ts | ~172 | sendKakaoNotification, sendPaidOrderKakao |
| payapp.ts | ~46 | parsePayappForm, verifyPayappLinkval |
| labels.ts | ~22 | getPlanLabel, getPayTypeLabel |

(굵은 글씨 = Phase 12에서 추가)

### Secrets (12개 + 자동 주입 2개)

```
KAKAO_REST_API_KEY          # 카카오 REST API 키
TELEGRAM_BOT_TOKEN          # 텔레그램 봇 토큰
TELEGRAM_CHAT_ID            # 사장님 텔레그램 ID
RESEND_API_KEY              # Resend API 키
RESEND_FROM_EMAIL           # noreply@blog.pluscoach.co.kr
DOWNLOAD_URL_FREE           # 무료체험 다운로드 URL
DOWNLOAD_URL_PAID           # 정식판 다운로드 URL
PAYAPP_USERID               # dhwnstjr00
PAYAPP_LINKKEY              # 페이앱 연동 KEY
PAYAPP_VALUE                # 페이앱 연동 VALUE
PAYMENT_MODE                # 'bank_transfer' | 'payapp' (결제 방식 스위치)
SUPABASE_DB_URL             # (미사용)
# SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY → Supabase 자동 주입
```

### Telegram Bot Webhook

```
URL: https://egwmkpplnzypkbedasrs.supabase.co/functions/v1/telegram-callback
설정일: 2026-04-18
확인: curl https://api.telegram.org/bot{TOKEN}/getWebhookInfo
```

---

## 6. 프론트엔드 구조 (현재)

### JS 로드 순서 (index.html)
```html
<script src="https://cdn.tailwindcss.com"></script>
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="https://lite.payapp.kr/public/api/v2/payapp-lite.js"></script>
...
<script src="assets/js/config.js"></script>     <!-- PLAN_CONFIG + APP_CONFIG + tracking -->
<script src="assets/js/tracking.js"></script>   <!-- 🆕 Phase 12A: 4개 플랫폼 통합 트래킹 -->
<script src="assets/js/main.js"></script>       <!-- UI + plan-label + phone 토글 + 트래킹 이벤트 3종 -->
<script src="assets/js/payment.js"></script>    <!-- PayApp (dormant) -->
<script src="assets/js/bank-transfer.js"></script>  <!-- 무통장 모달 + 트래킹 이벤트 2종 -->
<script src="assets/js/form.js"></script>       <!-- RPC + paymentMode 분기 + 트래킹 이벤트 1종 -->
```

### config.js 스위치 값

```js
window.APP_CONFIG = {
    supabase: { url, anonKey },
    kakao: { channelUrl },
    site: { url },
    paymentMode: 'bank_transfer',   // ← 스위치 지점
    bankInfo: { bank, account, holder },
    tracking: {                     // 🆕 Phase 12A
        ga4MeasurementId: 'G-YK4F3496T2',
        clarityProjectId: 'wdxl8sd38s',
        metaPixelId: '1652318165952879',
        googleAdsConversionId: 'AW-17816310398',
        googleAdsConversionLabel: 'GpYgCOzAwZ4cEP6kva9C'
    }
};
```

### 플랜 구성 (PLAN_CONFIG — SSOT)

| planKey | planCode | 금액 | UI | 만료일 |
|---------|----------|------|-----|--------|
| free | free_trial | 0원 | 숨김 | 24시간 |
| 1month | monthly | 39,000원 | 표시 | 31일 |
| full | full_package | 59,000원 | 표시 (추천) | 62일 |
| lifetime | lifetime | 255,000원 | 표시 | 2099-12-31 |

### 무통장 모달 (bank-transfer.js)

- IIFE 패턴, 전역 노출: `window.showBankTransferModal(ctx)`, `window.hideBankTransferModal()`
- 닫기: X 버튼 / 오버레이 클릭 / ESC
- **2단계 화면 전환** (Phase 12A Step F):
  - Screen 1 (`bank-screen-input`): 계좌 정보 + 빨간 버튼
  - Screen 2 (`bank-screen-success`): 체크 아이콘 + 영업시간 + 카톡 CTA + 스팸 안내
  - 빨간 버튼 클릭 → `notify-deposit-confirmed` POST → Screen 2 슬라이드 전환
  - 에러 시 기존 토스트 + 버튼 복원 (Screen 1 유지)
- CSS: `bank-` namespace로 기존 스타일과 격리

---

## 7. Phase 12 상세 작업 로그 (2026-04-18)

**목표:** 페이앱 PG 재승인 대기 중, 무통장 입금 임시 결제 플로우 구축.
**핵심 원칙:** 기존 코드 삭제 금지. 분기 추가 + 신규 파일만. 스위치 2곳으로 페이앱 즉시 복귀 가능.
**커밋:** 18개 (Step별 분할 커밋, `5cb80c4`~`0c3e4af`)

---

### Step 1: DB Migration — orders.phone + create_order 7파라미터

**파일:** `supabase/migrations/20260418120000_add_phone_and_extend_create_order.sql`
**커밋:** `5cb80c4`

**작업 내용:**
- `ALTER TABLE orders ADD COLUMN phone TEXT DEFAULT ''` — 기존 row는 빈 문자열 자동 채움
- `CREATE OR REPLACE FUNCTION create_order(...)` — 7파라미터 (p_phone 맨 뒤, DEFAULT '')
- 기존 6파라미터 함수 `DROP` + 7파라미터에 `GRANT EXECUTE TO anon, authenticated`
- 비즈니스 로직(중복 체크, order_code 생성, INSERT)은 기존 원본과 100% 동일

**설계 결정:**
- p_phone을 맨 뒤 optional로 추가하여 기존 호출부(form.js의 named parameter) 호환 유지
- PostgreSQL named parameter notation이라 기존 6파라미터 호출도 에러 없이 작동
- GRANT 시그니처가 변경되므로(7파라미터) 기존 6파라미터 GRANT는 무효 → 명시적 DROP 필수

**검증:** 6파라미터 호출(기존 호환), 7파라미터 호출(phone 포함), 중복 체크 에러, phone 저장 확인 — 9개 검증 쿼리 전부 통과

---

### Step 2: Edge Function 공유 모듈 확장

**파일:** `_shared/telegram.ts`, `_shared/resend.ts`
**커밋:** `8e74998`

**telegram.ts 추가 함수 (기존 2개 보존):**

| 함수 | 용도 | 반환 |
|------|------|------|
| `sendDepositNoticeWithButtons` | 사장님 텔레그램 인라인 버튼 2개 (승인/리마인더) | `{ok, messageId?}` |
| `editTelegramMessage` | 승인 후 메시지 편집 (replyMarkup optional) | boolean |
| `answerCallbackQuery` | 버튼 누른 뒤 토스트/로딩 종료 | boolean |
| `sendTelegramMessage` | 범용 전송 (force_reply, HTML 지원) | `{ok, messageId?}` |
| `deleteTelegramMessage` | 메시지 삭제 (미사용, future-proof) | boolean |
| `sendDepositNoticeInfoOnly` | 버튼 없는 정보용 알림 (dormant) | `{ok, messageId?}` |
| `sendDepositReceivedNotice` | 답장 연결용 짧은 알림 (버튼 없음) | boolean |
| `escapeHtml` | Telegram HTML 이스케이프 (내부) | string |

**resend.ts 추가 함수:**

| 함수 | 용도 | 반환 |
|------|------|------|
| `sendReminderEmail` | 입금 리마인더 이메일 (고객 대상, 계좌 정보 포함) | boolean |
| `sendEmergencyAlertEmail` | 처리 실패 긴급 알림 (사장님 대상, 디버그 컨텍스트 포함) | boolean |

**설계 결정:**
- 기존 함수 시그니처/본문 1바이트도 수정 안 함
- escapeHtml은 telegram.ts/resend.ts 양쪽에 로컬 정의 (export 안 함)
- FROM_HEADER 상수는 resend.ts 기존 것 재사용

---

### Step 3: telegram-callback Edge Function 신규

**파일:** `supabase/functions/telegram-callback/index.ts` (~663줄)
**커밋:** `8c6a08b` (초기) → `d052ee6` (리마인더 버튼 유지 패치)

**Telegram Bot Webhook 수신 → callback_query / message 분기:**

| action | callback_data 형식 | 처리 |
|--------|-------------------|------|
| `approve` | `approve:{orderCode}` | 라이선스 발급 + 이메일 + 재발송 버튼 표시 |
| `remind` | `remind:{orderCode}` | 리마인더 이메일 (status 유지, 버튼 유지) |
| `resend_same` | `resend_same:{orderCode}` | 같은 이메일로 인증키 재발송 |
| `resend_new` | `resend_new:{orderCode}` | force_reply로 새 이메일 입력 요청 |
| (message) | reply_to_message 기반 | 주문코드 파싱 → 이메일 검증 → 다른 이메일 재발송 |

**설계 결정:**
- 라이선스 재발송 시 기존 키 재사용 (새 발급 안 함) — orders.email도 미변경 (원본 기록 유지)
- 항상 200 반환 (Telegram 재시도 방지)
- 중복 승인 방지: `order.license_key` 있으면 `answerCallbackQuery("ℹ️ 이미 발급됨")` 후 return
- 모든 실패 경로에서 `sendEmergencyAlertEmail` 발송 (사장님 이메일 안전망)
- editTelegramMessage에 `replyMarkup` optional 추가 → handleRemind에서 버튼 유지, handleApprove에서 재발송 버튼으로 교체

**Step 3.5 패치 (`d052ee6`):**
- 문제: editMessageText가 인라인 키보드를 제거 → 리마인더 후 승인 불가
- 해결: editTelegramMessage에 replyMarkup optional 파라미터 추가. handleRemind에서만 원본 버튼 재전송, handleApprove에서는 재발송 버튼 표시

---

### Step 4: on-new-order 무통장 분기 추가

**파일:** `on-new-order/index.ts`
**커밋:** `176aa17`

**수정 내용:**
- import: `sendDepositNoticeWithButtons` 추가 (→ Step 6.6에서 → Step 6.7에서 최종 형태)
- 유료 분기 안에 `PAYMENT_MODE` 환경변수 체크:
  - `'bank_transfer'` → sendDepositNoticeWithButtons + telegram_notice_message_id 저장
  - `'payapp'` (기본값) → 기존 조기 반환 그대로
- 무료체험 로직(3중 알림 + 라이선스 + 이메일)은 1바이트도 변경 없음

**검증:**
- 무료체험 회귀 테스트: status='발송완료', license_key 자동 발급 ✅
- 무통장 유료 테스트: status 변경 + 텔레그램 알림 수신 ✅
- 페이앱 모드 회귀 (PAYMENT_MODE=payapp): status='결제대기' 유지, 알림 없음 ✅

**Step 4.5 — Trigger phone 추가 (`c4b1b75`):**
- 문제: 텔레그램 알림에 "전화: (미입력)" 표시 — Trigger payload에 phone 누락
- 파일: `supabase/migrations/20260418130000_notify_trigger_add_phone.sql`
- `CREATE OR REPLACE FUNCTION notify_new_order()` — jsonb_build_object에 `'phone', NEW.phone` 한 줄 추가
- CREATE TRIGGER 재실행 없음 (동일 시그니처 REPLACE로 기존 연결 유지)

---

### Step 5A: 프론트엔드 마크업

**파일:** config.js, index.html, main.css, main.js
**커밋:** `8b2946c` (초기) → `96b92ce` (CTA 빨간색 + 힌트 개선)

**config.js:**
- `APP_CONFIG.paymentMode: 'bank_transfer'` — 프론트 스위치 지점
- `APP_CONFIG.bankInfo: { bank, account, holder }` — 계좌 정보
- PLAN_CONFIG 변경 없음 (SSOT 보존)

**index.html:**
- phone input (`id="phone-field"`, 기본 `display:none`) — email과 plan 사이에 삽입
- Tailwind 클래스는 기존 name/email input과 동일
- 무통장 모달 (`id="bank-transfer-modal"`, `bank-` namespace) — paymentLoading 위에 삽입
  - 모달 구조: 배지 + 제목 + 주문 요약 + 계좌 정보 + 입금자명 안내 + 주문번호 + CTA 버튼 + 힌트
  - 접근성: `role="dialog"`, `aria-modal="true"`, `aria-label`

**main.css (추가 ~256줄):**
- `.bank-modal-overlay` — fixed, 반투명 배경, z-index 9999
- `.bank-modal-content` — 440px 최대폭, 20px radius, slideUp 애니메이션
- `.bank-btn-primary` — 빨간 그라디언트 (#dc2626→#ef4444), 16px 폰트
- `.bank-footer-hint` — 연한 초록 배경 박스, 영업시간 강조
- 모바일 반응형 (@media max-width: 480px)

**main.js (추가 ~14줄):**
- `paymentMode === 'bank_transfer'`이면 phone-field 표시 + `required = true`
- try/catch 감싸서 에러 시 다른 UI 로직에 영향 없음

**Step 5A 패치 (`96b92ce`):**
- CTA 버튼: 초록 → 빨간 그라디언트 (더 강한 행동 유도)
- 버튼 문구: "입금 완료 — 확인 요청하기" → "입금 완료 후 눌러주세요"
- 하단 힌트: `<p>` → `<div>` 3줄 분리, 영업시간 초록 굵게 강조

---

### Step 5B: 프론트엔드 JS 로직

**파일:** bank-transfer.js (신규 156줄), form.js (수정), index.html (script 태그 추가)
**커밋:** `9c8714c`

**bank-transfer.js:**
- IIFE 패턴, 전역 노출: `window.showBankTransferModal(ctx)`, `window.hideBankTransferModal()`
- DOMContentLoaded에서 모달 요소 바인딩
- 닫기 3종: X 버튼 / 오버레이 클릭 / ESC
- 입금 완료 버튼 → `notify-deposit-confirmed` Edge Function POST → 성공 토스트 + 2.5초 후 모달 닫기
- 네트워크 예외 시 버튼 재활성화 + 에러 토스트 + 재시도 안내
- Supabase anon key + Authorization Bearer 헤더로 Edge Function 호출

**form.js 수정:**
- phone 값 수집: `document.getElementById('phone').value.trim()`
- paymentMode='bank_transfer' + 유료 플랜이면 phone 필수 검증 (빈값 + 9자리 미만 체크)
- `create_order` RPC에 `p_phone: phone || ''` 추가 (7번째 파라미터)
- 유료 분기에서 paymentMode 체크:
  - `'bank_transfer'` → `window.showBankTransferModal(ctx)` 호출
  - `'payapp'` → 기존 `window.requestPayappPayment(...)` 호출 (100% 보존)
- `typeof` 체크로 함수 미정의 시 방어적 에러 처리

**index.html:**
- `<script src="assets/js/bank-transfer.js"></script>` — payment.js 다음, form.js 이전에 삽입
- 로드 순서: config → main → payment → bank-transfer → form

---

### Step 6: Telegram Bot Webhook 설정

**커밋:** 없음 (코드 변경 없이 API 호출만)

```bash
# 1. 상태 확인 → url: "" (미설정)
curl -s "https://api.telegram.org/bot{TOKEN}/getWebhookInfo"

# 2. Webhook 설정
curl -s -X POST "https://api.telegram.org/bot{TOKEN}/setWebhook" \
  -d "url=https://egwmkpplnzypkbedasrs.supabase.co/functions/v1/telegram-callback"

# 3. 확인 → url: ".../telegram-callback", pending_update_count: 0
```

설정 후 사장님 텔레그램에서 인라인 버튼 클릭 시 telegram-callback으로 callback_query 수신 가능.

---

### Step 6.5: 인증키 재발송 기능 추가

**파일:** `_shared/telegram.ts`, `telegram-callback/index.ts`
**커밋:** `d0436c6`

**추가 기능:**
- handleApprove 완료 후 텔레그램 메시지에 재발송 버튼 2개 표시:
  - `[🔄 같은 이메일 재발송]` → 기존 이메일로 같은 인증키 재발송
  - `[✏️ 다른 이메일로]` → force_reply로 새 이메일 입력 → 봇이 수신 → 형식 검증 → 재발송
- `_shared/telegram.ts`에 `sendTelegramMessage` (범용, force_reply 지원) + `deleteTelegramMessage` 추가
- telegram-callback에 `resend_same`, `resend_new` action 추가 + `handleIncomingMessage` (reply_to 기반 주문코드 파싱)
- `TelegramUpdate.message` 타입: `unknown` → `TelegramMessage` 인터페이스
- `buildResendButtons(orderCode)` DRY 헬퍼

---

### Step 6.6: 폼 제출/입금 완료 알림 2단계 분리

**커밋:** `0db0ff2` → `13fde59` → `c253518` → `e27d726` → `7c5747f` (5개)

**구조 변경 배경:**
- 기존: 폼 제출 시 바로 버튼 포함 알림 → 고객이 빨간 버튼 안 눌러도 사장님이 승인 가능하지만, "고객이 정말 입금했는지" 시그널 없음
- 변경: 폼 제출 시 정보용 알림 → 고객 빨간 버튼 시 답장 형태로 "입금 완료 신고" 알림

**A단계 (DB):**
- `20260418140000_add_telegram_notice_message_id.sql` — orders에 BIGINT NULL 컬럼 추가
- 용도: on-new-order가 발송한 텔레그램 message_id 저장 → notify-deposit-confirmed에서 reply_to_message_id로 사용

**B단계 (_shared/telegram.ts):**
- `sendDepositNoticeInfoOnly` 신규 — 버튼 없는 정보용 알림, messageId 반환 (→ Step 6.7에서 dormant 전환)
- `sendDepositNoticeWithButtons`에 `replyToMessageId?` optional 파라미터 추가 + `allow_sending_without_reply`

**C단계 (on-new-order):**
- 무통장 분기: sendDepositNoticeInfoOnly 호출 + telegram_notice_message_id 저장
- status는 '결제대기' 유지 (입금대기 변경은 notify-deposit-confirmed 담당)

**D단계 (notify-deposit-confirmed 신규):**
- `supabase/functions/notify-deposit-confirmed/index.ts` (~160줄)
- 고객 빨간 버튼 클릭 시 호출 (bank-transfer.js에서 POST)
- 멱등성: 이미 발급/입금대기 상태면 스킵 (중복 알림 방지)
- orders.status = '입금대기' UPDATE
- sendDepositNoticeWithButtons(replyToMessageId) → 답장 형태로 연결
- CORS preflight 지원 (프론트에서 직접 호출)
- config.toml에 `verify_jwt = false` 추가

**E단계 (bank-transfer.js):**
- handleDepositConfirm: sync → async, `notify-deposit-confirmed` Edge Function POST
- 성공/실패 모두 사용자에겐 친절 메시지, 실패 시 사장님 긴급 알림은 백엔드에서 처리

---

### Step 6.7: 알림 구조 재조정 (최종)

**커밋:** `d2fdbd3`

**변경 배경:** Step 6.6에서 첫 번째 알림이 버튼 없는 정보용이었는데, 사장님이 빨간 버튼 대기 없이 바로 승인할 수 있도록 첫 번째 알림에도 버튼 포함하는 것으로 변경.

**최종 알림 구조:**
1. **폼 제출 시** (on-new-order) → `💳 신규 주문 (입금 확인 필요)` **[✅ 승인] [📧 리마인더] 버튼 포함** + message_id 저장
2. **고객 빨간 버튼 시** (notify-deposit-confirmed) → `✅ 고객이 입금 완료 버튼을 눌렀습니다.` **답장 연결, 버튼 없음**
3. 사장님은 1번 알림의 승인 버튼을 바로 누를 수도 있고, 2번 답장 확인 후 누를 수도 있음

**코드 변경:**
- `sendDepositNoticeWithButtons` 반환 타입: `Promise<boolean>` → `Promise<{ok, messageId?}>` (message_id 회수 필요)
- `sendDepositNoticeWithButtons` 제목: '무통장 입금 확인 요청' → '신규 주문 (입금 확인 필요)'
- `sendDepositNoticeInfoOnly`: dormant 전환 (주석만 추가, 코드 보존)
- `sendDepositReceivedNotice` 신규: 답장 연결용 짧은 알림 (버튼 없음, ~47줄)
- on-new-order: `sendDepositNoticeInfoOnly` → `sendDepositNoticeWithButtons` 복원
- notify-deposit-confirmed: `sendDepositNoticeWithButtons` → `sendDepositReceivedNotice` 교체

---

### Step 6.8: 토스트 스팸함 안내 추가

**커밋:** `0c3e4af`

- 무료체험 성공 토스트: `+ 📮 이메일이 안 보이면 스팸함을 꼭 확인해주세요!`
- 무통장 입금 확인 토스트: 동일 문구 추가 + 모달 자동 닫힘 1.5초 → 2.5초 (읽을 시간 확보)

---

## 7A. Phase 12A 상세 작업 로그 (2026-04-19)

**목표:** 트래킹 시스템 구축 + 파비콘·OG 이미지 제작 + UX 보완.

### Step A: 폼 성공 토스트 스팸 안내 추가
- `assets/js/form.js` — 무료체험 성공 토스트에 `📮 이메일이 안 보이면 스팸함을 꼭 확인해주세요!` 추가
- `assets/js/bank-transfer.js` — 무통장 입금 확인 요청 토스트에 동일 문구 추가
- 무통장 모달 자동 닫힘 시간 `1.5s → 2.5s` (문구 읽을 시간 확보)

### Step B: 파비콘 + OG 이미지 제작
- 제미나이 AI 생성 B 로고 PNG (4색 리본 스타일) 기반
- Python(Pillow + scipy flood fill)로 체커보드 배경 제거 → 투명 PNG
- 파비콘 7종 (16/32/48/180/192/512 + ico 멀티사이즈)
- OG 이미지 1200x630 (아이보리 #F7F3EE 배경 + 네이버 초록 "비밀 무기" 블록 + 골드 #B8860B "◆ AI AUTO SOLUTION" VIP 라벨)
- 파일 위치: `image/` 폴더 → Step D에서 `assets/images/`로 이동 + 배포 완료

### Step C: 트래킹 시스템 4개 플랫폼 구축

**Step C-1: 구조 설계 (tracking.js + config.js)**
- `assets/js/tracking.js` 신규 생성 — 통합 트래킹 매니저
  - `window.track(eventName, params)` — 4개 플랫폼 동시 전송
  - `window.trackConversion(value, currency, orderCode)` — 결제 전환 전용 (Google Ads + Meta Purchase + GA4 purchase)
  - ID 빈 문자열이면 자동 비활성 (안전 fallback)
  - Meta 표준 이벤트 자동 매핑: `form_submit→Lead` / `bank_confirm_click→InitiateCheckout` / `payment_complete→Purchase`
- `assets/js/config.js` — `tracking` 섹션 추가 (ID 5개 필드)
- `index.html` — `<script src="assets/js/tracking.js">` 로드 추가 (config.js 바로 뒤)

**Step C-2: 이벤트 호출 지점 심기 (3개 파일)**
- `assets/js/main.js` (파일 끝 IIFE 블록 추가):
  - `plan_select` — 플랜 라디오 변경 시
  - `form_start` — 이름/이메일 첫 포커스 시 (세션당 1회)
  - `kakao_channel_click` — 카카오톡 링크 클릭 시
- `assets/js/form.js` — `form_submit` 이벤트 (무료체험 성공 토스트 직전)
- `assets/js/bank-transfer.js` — `bank_modal_view`, `bank_confirm_click` 이벤트

**Step C-3: 각 플랫폼 계정 생성 + ID 수집**
- **GA4** (`G-YK4F3496T2`): 플러스 코치 계정 / BlogAutoFriends 신규 속성 / 데이터 보관 14개월 설정 / 비즈니스 목표 리드 생성 + 판매 촉진
- **Microsoft Clarity** (`wdxl8sd38s`): "블로그 상세페이지" 신규 프로젝트 / B2C 서비스 카테고리
- **Meta Pixel** (`1652318165952879`): 플러스 코치 비즈니스 포트폴리오 내 "BlogAutoFriends Pixel" 신규 생성 / 기존 "준석's Pixel"과 분리
- **Google Ads** (`AW-17816310398` / 라벨 `GpYgCOzAwZ4cEP6kva9C`): 기존 jstv3457 계정 / "BAF 유료 결제 완료" 구매 전환 액션 / 전환별 다른 가치 / 1회 카운트

**Step C-4: config.js에 ID 5개 주입 + 배포**
- 커밋: `01b8335` - "feat: 트래킹 시스템 4개 플랫폼 ID 연결"
- 배포 후 브라우저 콘솔 로그 확인: `[tracking] 활성화 상태 - GA4: true Clarity: true Meta: true GoogleAds: true`

**Step C-5: 실시간 작동 검증 (2026-04-19)**
- GA4: 실시간 활성 사용자 2명 + form_start 이벤트 3회 도달 확인
- Clarity: 라이브 세션 2개 녹화 중 (Live 상태) 
- Meta Pixel: Pixel Helper 확장으로 Active + 1 event 확인
- Google Ads: 전환 태그 AW-17816310398 로드 확인, 실제 결제 대기

### Step D: 파비콘 + OG 이미지 배포 (v8 이월 작업 완료)

**커밋:** `8aa6b89` - "feat: 파비콘 + OG 이미지 배포"

**파일 이동:**
- `image/` → `assets/images/` 이동 (구조 일관성: assets/ 하위 통일)
- 중복 파일 2개 (`favicon-32x32 (1).png`, `logo_clean (1).png`) 삭제
- 최종 10개 파일: favicon.ico, favicon-{16x16,32x32,48x48}.png, apple-touch-icon.png, android-chrome-{192x192,512x512}.png, og-image.png, logo_clean.png, README_설치가이드.md

**index.html — 풀버전 메타태그 (27줄):**
- 파비콘 링크 6종 (32x32, 16x16, .ico, apple-touch-icon, 192x192, 512x512)
- `theme-color: #03C75A` (네이버 초록)
- Open Graph 10종 (type, site_name, title, description, url, image, image:width/height/alt, locale)
- Twitter Card 4종 (card, title, description, image)
- OG title: "상위 1% 블로거들의 비밀 무기"
- OG description: "AI가 사람처럼 천천히, 하루 100명을 소통합니다."

**privacy.html / terms.html / refund.html — 간소 메타태그 (12줄 각):**
- 파비콘 4종 (32x32, 16x16, .ico, apple-touch-icon) + theme-color
- Open Graph 6종 (type, site_name, title, url, image, locale)
- 각 페이지별 og:title / og:url만 차별화

**배포 검증:**
- `curl -sI og-image.png` → 200 OK (112KB)
- `curl -sI favicon.ico` → 200 OK (706B)
- `curl -s / | grep og:image` → 메타태그 정상 출력

---

### Step E: index.html `<title>` 태그 OG 제목 통일

**커밋:** `1f6eb94` - "feat: index.html `<title>` 태그를 OG 제목과 통일"

- 변경 전: `BlogAutoFriends — 네이버 블로그 서로이웃 자동 소통 AI`
- 변경 후: `상위 1% 블로거들의 비밀 무기`
- 목적: 브라우저 탭 제목과 카톡/페북 공유 썸네일 제목 일치 → 브랜딩 통일
- 법적 페이지(privacy/terms/refund) 3개는 기존 형식 유지 (수정 안 함)

---

### Step F: 무통장 모달 "입금 확인 완료 화면" 추가 (2단계 화면 전환)

**커밋:** `175abdb` - "feat: 무통장 모달 '입금 확인 완료 화면' 추가"

**변경 배경:**
- Before: 빨간 버튼 클릭 → 상단에 작은 토스트 → 2.5초 후 모달 자동 닫힘 (사용자가 안내 놓침)
- After: 빨간 버튼 클릭 → 모달 내부가 "접수 완료 화면"으로 슬라이드 전환 → 사용자가 직접 닫음

**index.html:**
- 기존 모달 내부를 Screen 1 (`bank-screen-input`, `data-screen="input"`) 컨테이너로 감쌈
- Screen 2 (`bank-screen-success`, `data-screen="success"`) 신규 추가
  - 큰 체크 아이콘 (SVG, 체크마크 그리기 애니메이션)
  - "입금 확인 요청이 접수되었습니다" 제목
  - 영업시간 안내 박스 (1시간 내 처리, 오전 10:00 ~ 오후 8:00)
  - 카카오톡 채널 CTA 버튼 (노란색, 화살표 호버 애니메이션)
  - 스팸함 안내 강조 박스 (노란 그라디언트 배경, 📮 아이콘)
  - "확인" 닫기 버튼
- X 버튼은 양쪽 Screen 공통 사용 (감싸지 않음)

**main.css (추가 ~272줄):**
- `.bank-modal-screen` — 전환 애니메이션 (`bankScreenFadeIn`, 0.35s 슬라이드)
- `.bank-success-check` — 체크 아이콘 팝 애니메이션 (`bankCheckPop`, cubic-bezier 바운스)
- `.bank-check-path` — SVG 체크마크 그리기 (`bankCheckDraw`, stroke-dashoffset)
- `.bank-success-info` — 영업시간 박스 (연한 초록 배경, 점선 테두리)
- `.bank-success-kakao-btn` — 카카오 노란 버튼 (호버 시 그림자 + 화살표 이동)
- `.bank-success-spam` — 스팸 안내 (노란 그라디언트 + 주황 테두리)
- `.bank-success-close-btn` — 회색 확인 버튼
- 모바일 반응형 (@media max-width: 480px)

**bank-transfer.js (4개 변경):**
1. `showScreen(screenName)` 헬퍼 함수 추가 — `data-screen` 속성 기반 화면 전환
2. DOMContentLoaded에 Screen 2 "확인" 버튼 (`bank-success-close-btn`) → `hideBankTransferModal` 바인딩
3. `showBankTransferModal` 내부: `showScreen('input')` 호출 (모달 열 때 항상 Screen 1로 초기화)
4. `handleDepositConfirm` 성공 시: 기존 토스트 + setTimeout 자동닫힘 → `showScreen('success')` 교체
   - 에러 시 기존 토스트 + 버튼 복원 로직은 그대로 유지

---

### Step G: 긴급 알림 이메일 2종만 유지 + 사장님 친화적 개선

**커밋:** `4f68caa` - "refactor: 긴급 알림 이메일 2종만 유지 + 사장님 친화적으로 개선"

**변경 배경:**
- Before: 17개 `sendEmergencyAlertEmail` 호출이 전부 발송 → 사장님 이메일 폭탄
- Before: 기술 용어 그대로 노출, 주문 정보 없음, "뭘 해야 하는지" 가이드 없음
- After: 중요한 2개만 유지, 나머지 15개 주석 처리

**유지된 2개 (telegram-callback/index.ts):**

| errorType | 심각도 | 발생 시점 |
|-----------|--------|----------|
| `LICENSE_CREATE_FAILED` | 🔴 긴급 | 승인 버튼 → create_license RPC 실패 |
| `EMAIL_SEND_FAILED` | 🟡 주의 | 승인 성공 → 인증키 이메일 Resend 실패 |

**주석 처리된 15개:**
- notify-deposit-confirmed/index.ts: 5개 (주문 조회 실패, DB UPDATE 실패, message_id 누락, 텔레그램 답장 실패, 전체 예외)
- telegram-callback/index.ts: 10개 (주문 조회 실패, DB UPDATE, DOWNLOAD_URL_PAID 미설정, handleApprove 예외, 리마인더 실패, handleRemind 예외, resend_same 3개, resend_new 2개)

**resend.ts 함수 전면 개편:**
- `EmergencyErrorType` 타입 추가 (`LICENSE_CREATE_FAILED` | `EMAIL_SEND_FAILED`)
- `EmergencyOrderInfo` 인터페이스 추가 (name, email, phone, plan, amount, order_code, created_at, license_key)
- 심각도별 색상 배너 (빨강 🔴 / 노랑 🟡)
- 주문 정보 풀 표시 (이름·이메일·전화 tel: 링크·플랜·금액·주문시각·라이선스)
- "무슨 일이 일어났나요?" 쉬운 한국어 설명
- "지금 해야 할 일" 번호 매긴 단계별 가이드
- 기술 정보는 `<details>` 접힘 처리 (개발자용)
- `subject` 파라미터는 하위호환용으로 유지하되 무시됨

**Edge Function 배포:** notify-deposit-confirmed + telegram-callback 2개 함수 배포 완료

---

### Step H: Google Ads 광고 정책 위반 소지 문구 정리

**커밋:** `50b3613` - "fix: Google Ads 정책 위반 소지 문구 정리"

**변경 배경:** Google Ads 정책 심사 시 "SNS 자동화 도구 판매"로 분류되어 반려될 위험. 핵심 용어를 정책 친화적으로 치환.

**index.html 주요 치환 (20곳):**

| 변경 전 | 변경 후 | 위치 |
|---------|---------|------|
| 서로이웃 **자동 추가** | 서로이웃 **관리** | meta description |
| **자동화** 솔루션 | **관리** 솔루션 | og:description |
| 서이추 **자동화** | 서이추 **AI 관리** | 히어로 배지 |
| **AI 자동화** 소통 | **AI 소통 관리** | 히어로 부제 |
| 하루 100명 **자동** 소통 | 하루 100명 **AI** 소통 | 히어로 배지 |
| 이웃 수락률 **87%** | **높은** 이웃 수락률 | 히어로 배지 |
| **'완전 자동화'** | **'AI 관리 시대'** | 섹션 타이틀 |
| **자동** 발송 | **선별** 발송 | STEP2 설명 |
| 48명 **자동** 발송 중 | 48명 발송 중 | STEP2 UI |
| 6단계 **자동화** 시스템 | 6단계 **AI 관리** 시스템 | 6단계 버튼 |
| 6단계 **자동화** | 6단계 **소통 관리** | 6단계 제목 |
| **완벽하게** 파악 | **정확히** 파악 | STEP04 |
| **수익이 나는** 공간 | **성장하는** 공간 | 후기 마무리 |
| **인간 패턴 완벽 구현** | **인간 패턴 정밀 구현** | 안전 카드 |
| **99.8%** / **제재 사례 0건** | **높음** / **안전하게 운영 중** | 안전 스코어 |
| **100% 안심 환불 보장** | **안심 환불 정책** | 환불 배너 |
| **0건/100%** | **🛡️/24h** | 환불 배너 수치 |
| 3,100명 / **자동 신청** | **최대** 3,100명 / **AI 소통** | 1개월 플랜 |
| 6,000명 / **자동 신청** | **최대** 6,000명 / **AI 소통** | 풀패키지 |
| **매크로와 달리...매우 안전** | 사람처럼 **자연스럽게 동작** | FAQ |
| 블로그 **자동화** 솔루션 | 블로그 **AI 소통** 솔루션 | 푸터 |

**config.js 플랜 이름 (3곳):**
- "블로그 **자동화** 1개월/풀패키지/평생 소유권" → "블로그 **AI 솔루션** 1개월/풀패키지/평생 소유권"

**유지 결정 항목:**
- "상위 1% 블로거들의 비밀 무기" — 열망 마케팅 비유, 정책 위반 아님
- "자동 종료/자동 선별" — 프로그램 내부 기능 설명
- "자동 발송" (1372줄) — 이메일 발송, SNS 아님

---

### Step I: robots.txt 추가 — 네이버 검색 봇(Yeti) 차단

**커밋:** `faa2625` - "feat: robots.txt 추가 — 네이버 검색 봇(Yeti) 차단"

**배경 조사:**
- 네이버 어뷰징 대응팀이 "블로그 자동화" 키워드를 주기적으로 검색하여 판매 사이트 적발
- 네이버가 자동화 프로그램 판매업체를 정보통신망법 제48조 위반으로 형사 고발한 사례 다수
- 벌칙: 5년 이하 징역 또는 5천만원 이하 벌금
- 네이버 검색에 노출되는 것이 가장 큰 위험 요소

**robots.txt 내용:**
```
User-agent: Yeti
Disallow: /
```
- 네이버 검색 봇(Yeti)만 차단, 나머지(구글/메타/카카오 등) 전부 허용
- 구글 검색 SEO 유입 가능 + 광고 정상 작동 + 카톡 공유 썸네일 정상

**광고 봇과의 관계 (공식 문서 확인):**
- Google Ads: `AdsBot-Google` (별도 봇, Googlebot과 분리) → 차단 안 함 → 광고 정상
- Meta Ads: `facebookexternalhit` → 차단 안 함 → 광고 정상
- 공식 출처: developers.google.com/search/docs/crawling-indexing/overview-google-crawlers

**한계:**
- robots.txt는 크롤링 기반 노출만 방지
- 신고 시 네이버 직원이 직접 URL 접속 → robots.txt 무시 → 법적 조치 가능
- 광고 클릭한 사용자 중 누군가가 네이버에 신고할 가능성은 여전히 존재

---

### Step J: OG 이미지 캐시 무효화

**커밋:** `9505494` - "fix: OG 이미지 캐시 무효화 (?v=2)"

- 4개 HTML(index/privacy/terms/refund)의 og:image, twitter:image URL에 `?v=2` 쿼리 추가
- 카톡/페북/구글 캐시가 새 OG 이미지를 강제 로드하도록 유도
- 파일 자체는 그대로 (URL만 변경해서 캐시 우회)
- 검증: `curl -sI og-image.png?v=2` → 200 OK

---

### 발생한 이슈와 해결
- **이슈 A**: 제미나이 PNG가 RGBA가 아니라 체커보드 배경이 픽셀로 그려짐
  - 해결: Python flood fill로 외곽 배경 + B 내부 구멍 2곳까지 정확히 투명 처리
- **이슈 B**: 본인 브라우저에서 Network 탭에 `facebook` 요청 0개
  - 원인: 광고 차단 확장 프로그램
  - 확인: `typeof window.fbq === 'function'` 반환 → Meta Pixel Helper로 최종 검증
- **이슈 C**: Google Ads 기존 자동매매 계정의 금융 반려 이력
  - 결정: 정지 아닌 단순 반려이므로 기존 계정에서 별도 전환 액션만 분리 생성

---

## 8. 보안 가드레일

### 절대 금지
- licenses 테이블 구조 변경
- pluscoach.co.kr 루트 DNS 변경
- service_role_key를 프론트/Git에 노출
- 무료체험 코드 삭제 (visible 플래그로 숨김만)

### 필수 규칙
- Edge Function 배포 시 `--no-verify-jwt` 필수
- 운영 DB migration push 전 사장님 확인
- 가격 변경은 config.js PLAN_CONFIG에서만 (SSOT)
- planCode 추가 시 6곳 동시: create_license / orders CHECK / config.js / form.js / labels.ts / resend.ts
- 모든 Webhook/Edge Function은 항상 200 반환 (재시도 방지)
- Resend 이메일 디자인 토큰 / RFC 2047 from 헤더 건드리지 말 것
- 파비콘 경로 `/assets/images/` 변경 금지 (404 발생) — 가드레일 29
- OG 이미지 수정 시 파일명 버전 접미사 또는 `?v=N` 쿼리 필수 — 가드레일 30
- robots.txt에서 Yeti(네이버) 차단 유지 필수 — 네이버 검색 노출 시 법적 리스크
- 랜딩 페이지에 "자동화/자동 추가/매크로/봇" 등 SNS 조작 키워드 사용 금지 — Google Ads 반려 + 네이버 단속 리스크

---

## 9. 파일 구조 (현재)

```
blogautopage/
├── index.html                          # 랜딩 (phone 필드 + 무통장 2단계 모달 + 카카오 플로팅 + 파비콘/OG/Twitter)
├── privacy.html / terms.html / refund.html   # 법적 페이지 (간소 파비콘 + OG)
├── robots.txt                          # 🆕 v9: 네이버 봇(Yeti) 차단
├── CLAUDE.md                           # 리포 가드레일
├── CNAME                               # blog.pluscoach.co.kr
│
├── assets/
│   ├── images/                         # 🆕 v8: 파비콘·OG 이미지 (배포 완료)
│   │   ├── favicon.ico                 # 멀티사이즈 ICO
│   │   ├── favicon-{16x16,32x32,48x48}.png
│   │   ├── apple-touch-icon.png        # 180x180
│   │   ├── android-chrome-{192x192,512x512}.png
│   │   ├── og-image.png                # 1200x630 OG 이미지
│   │   ├── logo_clean.png              # B 로고 원본
│   │   └── README_설치가이드.md
│   ├── css/
│   │   ├── main.css                    # 통합 CSS + 무통장 모달 + Screen 2 완료 화면 스타일
│   │   ├── legal.css                   # 법적 페이지
│   │   └── floating-kakao.css          # 카카오 플로팅
│   └── js/
│       ├── config.js                   # PLAN_CONFIG + APP_CONFIG + tracking(ID 5개)
│       ├── tracking.js                 # 🆕 v8: 4개 플랫폼 통합 트래킹 매니저
│       ├── main.js                     # UI + phone 토글 + 트래킹 3종 (plan_select, form_start, kakao_channel_click)
│       ├── payment.js                  # PayApp (dormant)
│       ├── bank-transfer.js            # 무통장 모달 2단계 + 트래킹 2종 (bank_modal_view, bank_confirm_click)
│       └── form.js                     # RPC + paymentMode 분기 + 트래킹 1종 (form_submit) + 스팸 안내
│
├── supabase/
│   ├── config.toml                     # 4개 Edge Function verify_jwt=false
│   ├── migrations/
│   │   ├── 20260409000100~000500       # Phase 3: 기초 스키마 (5개)
│   │   ├── 20260410120000              # Phase 10.5: lifetime 기간
│   │   ├── 20260414120000              # Phase 10.5: CHECK 제약
│   │   ├── 20260418120000              # Phase 12: phone + 7파라미터
│   │   ├── 20260418130000              # Phase 12: Trigger phone
│   │   └── 20260418140000              # Phase 12: telegram_notice_message_id
│   └── functions/
│       ├── on-new-order/index.ts
│       ├── payapp-webhook/index.ts     # dormant
│       ├── telegram-callback/index.ts
│       ├── notify-deposit-confirmed/index.ts
│       └── _shared/
│           ├── telegram.ts  (474줄, 10 함수)
│           ├── resend.ts    (v2: EmergencyErrorType 2종 분기, 주문정보 풀표시)
│           ├── kakao.ts     (172줄, 2 함수)
│           ├── payapp.ts    (46줄)
│           └── labels.ts    (22줄)
│
├── backup/                             # Phase 6, 10 스냅샷
├── work/                               # 작업 지시서
└── doc/
    ├── 구현_과정_가이드.md               # 원본 (Phase 1~12 전체, 보존)
    ├── 구현_과정_가이드1.md              # Part 1 아카이브 (Phase 1~11)
    ├── 구현_과정_가이드2.md              # 이 파일 (현재 시스템)
    └── 진행상황.md / 인수인계 문서들
```

---

## 10. 다음 작업 후보

### ✅ 완료: 파비콘 + OG 이미지 배포 (Phase 12A Step D)
### ✅ 완료: 긴급 알림 이메일 정리 (Phase 12A Step G)
### ✅ 완료: 광고 정책 문구 정리 (Phase 12A Step H)
### ✅ 완료: 네이버 검색 차단 robots.txt (Phase 12A Step I)
### ✅ 완료: OG 이미지 캐시 무효화 (Phase 12A Step J)

### Phase 13: 광고 집행 준비
1. 결제 완료 이벤트 (payment_complete) 연결
   - 옵션 A: 서버 측 (telegram-callback handleApprove) - 정확, 1시간 작업
   - 옵션 B: 프론트 측 (bank-transfer handleDepositConfirm) - 빠름, 5분, 부정확
   - 옵션 C: 하이브리드 (B로 시작 → A로 업그레이드)
2. 광고 소재 제작 (Meta 이미지/동영상 + Google Display 배너)
3. 리타겟팅 맞춤 타겟 생성 (플랜 선택했지만 결제 안 한 사용자 등)
4. 일일 테스트 예산 + 플랫폼별 분배 (메타 70% / 구글 30% 추천)

### Phase 14: 광고 최적화 (광고 데이터 2~4주 쌓인 후)
- Clarity 세션 녹화 분석 → UX 개선
- Meta Conversions API 추가 (iOS 추적 ~70% → ~95%)
- 랜딩페이지 A/B 테스트

### 페이앱 복귀 시 (별도 트랙)
1. 페이앱 PG 재승인 완료 확인
2. `PAYMENT_MODE=payapp` + `paymentMode: 'payapp'` 스위치
3. E2E 테스트 (1,000원 테스트 결제)
4. 무통장 코드 dormant 확인

### 운영 보완 (우선순위 낮음)
- [ ] 카카오 REST API 키 재발급 (보안 정리)
- [ ] 텔레그램 봇 토큰 재발급
- [ ] Edge Function Secrets 교체
- [ ] Supabase CLI v2.84.2 → v2.90.0 업데이트
- [ ] 통신판매업 주소 변경 신고 (강동구 → 관악구)
- [ ] 문서 재편 (v3~v8 → PLAYBOOK + STATE 2파일 통합)
