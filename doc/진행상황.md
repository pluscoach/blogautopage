# BlogAutoFriends 진행 상황

## 완료
- Phase 1: 구조 리팩터링 (assets/css/main.css, assets/js/{config,main,form}.js 분리) ✅
- Phase 2: Supabase CLI 설정 (egwmkpplnzypkbedasrs 프로젝트 link 완료) ✅
- Phase 3: DB 스키마 (orders, kakao_tokens 테이블 + create_order, create_license RPC + 
  Database Trigger + pg_net + supabase_vault 설치) ✅
  - service_role_key는 Vault에 저장됨 (name='service_role_key')
- Phase 4: 폼 연결 (form.js create_order RPC 호출 방식, config.js에 URL+anon key 입력 완료) ✅
  - 로컬 테스트 통과 (무료체험/중복차단/유료 모달)
- Phase 5-A: Edge Function 코드 작성 (index.ts + _shared/{kakao,telegram,resend}.ts) ✅
  - kakao.ts의 link URL을 https://blog.pluscoach.co.kr 로 수정 완료 (수정 완료 시)
  - 배포는 미진행 (사전 준비물 대기)

## 중요 설정값
- 운영 Supabase 프로젝트 Ref: egwmkpplnzypkbedasrs
- Supabase URL: https://egwmkpplnzypkbedasrs.supabase.co
- 이메일 발신: noreply@blog.pluscoach.co.kr (Resend 도메인 인증 완료)
- Resend Region: Tokyo (ap-northeast-1)

## 다음 할 일 (Phase 5-B)
사전 준비 필요:
1. 텔레그램 봇 생성 (@BotFather) → TELEGRAM_BOT_TOKEN
2. 텔레그램 chat_id 확인 (@userinfobot) → TELEGRAM_CHAT_ID
3. Kakao Developers 앱 등록 → KAKAO_REST_API_KEY
4. Kakao 초기 access_token + refresh_token 발급
5. kakao_tokens 테이블에 초기 토큰 INSERT (SQL Editor)
6. Supabase Edge Function Secrets 설정:
   - KAKAO_REST_API_KEY
   - TELEGRAM_BOT_TOKEN
   - TELEGRAM_CHAT_ID
   - RESEND_API_KEY
   - RESEND_FROM_EMAIL=noreply@blog.pluscoach.co.kr
7. Edge Function 배포: supabase functions deploy on-new-order --no-verify-jwt
8. 실제 폼 제출 테스트 → 카카오/텔레그램/이메일 3개 수신 확인

## 건드리지 말 것 (가드레일)
- licenses 테이블 (기존 프로그램용, 원본 그대로 유지 중)
- pluscoach.co.kr 루트 도메인 DNS
- service_role_key는 Edge Function Secrets + Vault에만

## 참고 대화창
설계 대화창(Claude.ai)에서 단계별 검수 받으며 진행 중.
