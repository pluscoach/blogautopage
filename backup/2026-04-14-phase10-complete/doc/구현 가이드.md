# BlogAutoFriends 랜딩페이지 구현 가이드
## 2026-04-10 최신화

---

## 1. 기술 스택 & 아키텍처

### 프론트엔드
- **HTML** (`index.html`) — 메인 랜딩페이지
- **Tailwind CSS CDN** (`cdn.tailwindcss.com`) — 커스텀 CSS 최소화
- **Pretendard 폰트** (CDN) — 한국어 최적화
- **Supabase JS 클라이언트** (CDN `@supabase/supabase-js@2`) — RPC 호출
- **외부 CSS/JS 분리** — `assets/css/main.css`, `assets/js/{config,main,form}.js`
- **법적 페이지** — `privacy.html`, `terms.html`, `refund.html` + `assets/css/legal.css`

### 백엔드
- **Supabase** (프로젝트 Ref: `egwmkpplnzypkbedasrs`)
  - PostgreSQL DB — orders, kakao_tokens 테이블
  - RPC 함수 — create_order (anon), create_license (service_role)
  - Database Trigger — orders INSERT → Edge Function 호출
  - Edge Functions (Deno/TypeScript) — 알림 3중 발송 + 무료체험 자동 라이선스 발급
  - pg_net 확장 — HTTP 호출
  - supabase_vault — service_role_key 안전 보관

### 알림 시스템 (3중)
- **카카오톡 "나에게 보내기"** — 사장님 알림 (메인)
- **텔레그램 봇** — 사장님 알림 (백업)
- **Resend 이메일** — 사용자 접수 확인 + 인증키 발급 (발신: `noreply@blog.pluscoach.co.kr`)

### 배포
- **GitHub Pages** + 커스텀 도메인 `blog.pluscoach.co.kr` (예정)
- **카페24** DNS 관리 (CNAME: `blog` → `jsrb0.github.io`)

---

## 2. 폴더 구조 (현재)

```
blogautopage/
│
├── CLAUDE.md                                    # 리포 가드레일 (Claude Code용)
├── index.html                                   # 메인 랜딩페이지
├── index copy.html                              # 원본 백업 (리팩터링 전)
├── privacy.html                                 # 개인정보처리방침 (Phase 7)
├── terms.html                                   # 이용약관 (Phase 7)
├── refund.html                                  # 환불규정 (Phase 7)
├── .gitignore                                   # Git 제외 설정
│
├── assets/
│   ├── css/
│   │   ├── main.css                             # 통합 CSS (style 블록 7개 합침)
│   │   └── legal.css                            # 법적 페이지 공통 스타일 (Phase 7)
│   └── js/
│       ├── config.js                            # APP_CONFIG: Supabase URL/key, 플랜 정보
│       ├── main.js                              # UI 로직: 스크롤, 토스트, 애니메이션, 타이핑
│       └── form.js                              # Supabase RPC 호출, getUserIP, submitForm
│
├── supabase/
│   ├── .gitignore                               # supabase 자체 gitignore
│   ├── config.toml                              # [functions.on-new-order] verify_jwt = false
│   ├── migrations/
│   │   ├── 20260409000100_orders_table.sql      # orders 테이블 + RLS + updated_at 트리거
│   │   ├── 20260409000200_create_order_rpc.sql  # create_order RPC (anon GRANT)
│   │   ├── 20260409000300_create_license_rpc.sql # create_license RPC (service_role 전용)
│   │   ├── 20260409000400_kakao_tokens.sql      # kakao_tokens 테이블 (단일 행 제약)
│   │   └── 20260409000500_notify_trigger.sql    # pg_net + vault + DB Trigger
│   └── functions/
│       ├── on-new-order/
│       │   └── index.ts                         # 메인 Edge Function (145줄, Phase 6 무료체험 분기 포함)
│       └── _shared/
│           ├── kakao.ts                         # 카카오 "나에게 보내기" + 토큰 자동갱신 (128줄)
│           ├── telegram.ts                      # 텔레그램 봇 발송 (42줄)
│           └── resend.ts                        # Resend 이메일 (256줄, 접수확인 + 인증키 2개 함수)
│
├── doc/
│   ├── 인수인계_v3_FINAL.md                      # 마스터 설계 문서
│   ├── 인수인계_v4_Phase5완료.md                  # Phase 5 완료 인수인계
│   ├── 사전준비_체크리스트_FINAL.md                # 자격 증명 준비 체크리스트
│   ├── Claude_Code_시작_프롬프트_FINAL.md          # Claude Code 시작 프롬프트
│   ├── 구현 가이드.md                             # 이 파일 (기술 상세 문서)
│   ├── 구현_과정_가이드.md                         # Phase 1~5 작업 로그 + 재사용 템플릿
│   ├── BlogAutoFriends_백엔드_연동_가이드.md       # 설계 대화창 산출물
│   ├── 자동 결제 프로그램 배포 가이드.md            # 결제 자동화 참고
│   └── 진행상황.md                                # Phase별 진행 현황
│
├── backup/
│   ├── 2026-04-03/                               # 리팩터링 전 원본 백업
│   └── 2026-04-10-phase6-complete/               # Phase 6 완료 시점 전체 스냅샷
│
├── 설계 가이드.md                                 # 초기 설계 문서
├── 인수인계_결제자동화(04.07).md                   # v1 인수인계
├── 인수인계_추가 정보 및 보안 강화(04.08).md        # v2 인수인계
├── 제민나이3.html                                 # 디자인 참고용 원본
└── 초반후킹.html                                  # 참고용
```

---

## 3. Phase별 구현 히스토리

### Phase 1: 구조 리팩터링 ✅ (2026-04-09)

**목적:** 1724줄 단일 index.html을 유지보수 가능한 구조로 분리.

**작업 내용:**
1. `<style>` 블록 7개 → `assets/css/main.css`로 통합 (68줄)
2. 메인 `<script>` 블록 → 3개 JS 파일로 분리 (config, main, form)
3. index.html 정리: 인라인 style/script 삭제, 외부 파일 링크
4. 결과: 1724줄 → 1407줄 (317줄 감소)

---

### Phase 2: Supabase CLI 설정 ✅ (2026-04-09)

**목적:** 로컬에서 Supabase를 관리하고 배포할 수 있는 환경 구축.

**작업 내용:**
1. Supabase CLI 설치 (Scoop, v2.84.2)
2. `supabase login` + `supabase init` + `supabase link`
3. `config.toml` 수정 — `verify_jwt = false`
4. 프로젝트 Ref 구→신 14건 치환

---

### Phase 3: DB 스키마 ✅ (2026-04-09)

**목적:** orders 테이블, kakao_tokens 테이블, RPC 함수들, Database Trigger 생성.

**Migration 5개:** orders 테이블, create_order RPC, create_license RPC, kakao_tokens 테이블, notify_trigger

**Vault 설정:** `vault.create_secret('실제키', 'service_role_key')` 완료

---

### Phase 4: 폼 연결 ✅ (2026-04-09)

**목적:** 랜딩페이지 폼 → Supabase RPC 실제 연동.

- config.js에 URL/anonKey 입력
- form.js: `rpc('create_order')` 방식으로 교체 + getUserIP + userAgent
- 검증 완료: 무료체험/중복차단/유료모달

---

### Phase 5: Edge Function + 3중 알림 ✅ (2026-04-09~10)

**목적:** 주문 INSERT 시 3중 알림 자동 발송.

**작성 파일:** index.ts, kakao.ts, telegram.ts, resend.ts

**사전 준비 완료:** 텔레그램 봇, 카카오 앱+토큰, Resend 도메인 인증, Secrets 6개

**배포:** `supabase functions deploy on-new-order --no-verify-jwt`

**실전 테스트 통과:** 카카오톡/텔레그램/이메일 3중 수신 확인

---

### Phase 6: 무료체험 자동화 ✅ (2026-04-10)

**목적:** 무료체험 신청 시 자동 라이선스 발급 + 인증키 이메일 발송.

**index.ts 변경 (73줄 → 145줄):**
- `createClient` import 추가 (service_role로 DB 접근)
- `if (record.plan === 'free_trial')` 분기 구현
- create_license RPC 호출 → data를 TEXT 문자열로 직접 사용
- orders UPDATE: license_key + status='발송완료' 한 번에
- 필수 필드 방어 가드 (email/name/order_code 누락 시 status='데이터오류')
- 에러 처리: 발급실패 → status='발급실패', 이메일실패 → status='이메일실패'
- UPDATE 실패 시 텔레그램 긴급 알림
- DOWNLOAD_URL_FREE 환경변수 미설정 방어

**resend.ts 변경 (76줄 → 256줄):**
- `sendLicenseKeyEmail()` 함수 추가
- 이메일 전면 리디자인 (Phase 6.5):
  - 랜딩페이지 톤 맞춤: 베이지 배경(#faf9f6), 흰 카드(20px radius), 초록 알약 뱃지
  - 접수 확인: 다음 단계 안내 + 24h 카운트다운 예고 + 스팸함 안내
  - 인증키: 그라디언트 CTA + 검정 가이드 버튼 + 주황 경고 박스(카운트다운+환불) + 사용법 3단계(원형 번호 뱃지) + 실행 전 확인 노랑 박스 + 혜택 표(추천 뱃지) + 카카오톡 알약 버튼
- From 헤더 RFC 5322 준수: `=?UTF-8?B?...?=` MIME encoded-word
- 노션 가이드 버튼 추가

**Edge Function Secrets 추가:** `DOWNLOAD_URL_FREE`

---

### Phase 7: 사업자 푸터 + 법적 페이지 ✅ (2026-04-10)

**목적:** 전자상거래법 준수.

**작성 파일 4개:**

| 파일 | 내용 |
|------|------|
| `assets/css/legal.css` | 공통 스타일 (베이지 배경, 흰 카드, 구분선, 사업자 푸터 박스) |
| `privacy.html` | 개인정보처리방침 8개 조항 (수집항목, 이용목적, 보유기간, 제3자제공, 위탁, 이용자권리, 보호책임자, 고지의무) |
| `terms.html` | 이용약관 11개 조항 (목적, 용어, 효력, 서비스제공, 계약성립, 이용자의무, 회사의무, 이용제한, 면책, 분쟁해결, 저작권) |
| `refund.html` | 환불규정 6개 조항 + 상단 강조 요약 (무료체험, 유료환불, 예외사유, 청약철회, 신청방법, 처리기간) |

**index.html 푸터 교체:**
- 사업자 정보 5줄 (상호, 대표자, 사업자번호, 통신판매업번호, 주소)
- 법적 페이지 링크 3개 (개인정보처리방침/이용약관/환불규정) + 카카오톡 문의
- 가운데 정렬 (text-center + justify-center)

**사업자 정보:**
- 상호: 제이에스코퍼레이션 / 대표자: 오준석
- 사업자등록번호: 850-38-01085
- 통신판매업 신고번호: 제 2023-서울강동-1311호
- 주소: 서울특별시 관악구 남부순환로 1921-1, 401-A4호
- 이메일: jscorpor88@gmail.com

---

### Phase 8: GitHub Pages 배포 (진행 중)

- [x] .gitignore 보안 점검 + 누락 항목 추가 (supabase/.branches/, node_modules/, .DS_Store, .vscode/, .idea/)
- [x] Git push 완료 (c24e0f8..64a1fc4)
- [ ] GitHub Pages 활성화
- [ ] 카페24 CNAME 레코드 추가 (blog → jsrb0.github.io)
- [ ] 커스텀 도메인 설정 + HTTPS

---

## 4. DB 스키마 (운영 현황)

### 테이블

| 테이블 | 상태 | 용도 |
|---|---|---|
| `licenses` | 기존 (절대 건드리지 않음) | 프로그램 라이선스 관리, 9개 컬럼 |
| `orders` | Phase 3에서 생성 | 주문/신청 관리 |
| `kakao_tokens` | Phase 3에서 생성 | 카카오 토큰 관리 (단일 행) |

### RPC 함수

| 함수 | 권한 | 용도 |
|---|---|---|
| `verify_and_activate(key, machine_id)` | anon | 프로그램 라이선스 검증 (기존) |
| `update_session(key, machine_id)` | anon | 프로그램 세션 갱신 (기존) |
| `clear_session(key)` | anon | 프로그램 세션 정리 (기존) |
| `create_order(p_name, p_email, p_plan, p_amount, p_ip, p_user_agent)` | anon | 랜딩페이지 주문 (신규) |
| `create_license(p_buyer_name, p_plan, p_order_code)` | service_role | 라이선스 발급 (신규) |

### Trigger

| 트리거 | 테이블 | 이벤트 | 동작 |
|---|---|---|---|
| `on_orders_updated` | orders | BEFORE UPDATE | updated_at 자동 갱신 |
| `on_order_inserted` | orders | AFTER INSERT | Edge Function 호출 (net.http_post) |

---

## 5. 환경변수 & 시크릿

### config.js (프론트엔드, 공개 OK)
```
APP_CONFIG.supabase.url = 'https://egwmkpplnzypkbedasrs.supabase.co'
APP_CONFIG.supabase.anonKey = '실제_ANON_KEY'
```

### Supabase Vault (DB 내부)
```
name='service_role_key' → Database Trigger에서 사용
```

### Edge Function Secrets (등록 완료)
```
KAKAO_REST_API_KEY=...
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHAT_ID=...
RESEND_API_KEY=...
RESEND_FROM_EMAIL=noreply@blog.pluscoach.co.kr
DOWNLOAD_URL_FREE=https://drive.google.com/drive/folders/...
SUPABASE_URL=... (자동 주입)
SUPABASE_SERVICE_ROLE_KEY=... (자동 주입)
```

---

## 6. 프론트엔드 섹션 구조 (index.html)

### 현재 섹션 순서 (위→아래)
1. **히어로** — 태그라인 + 헤드라인 + 수치 뱃지 + AI 타이핑 데모
2. **문제 공감 (STEP 1/2/3)** — 서이추 무반응 / 방문자 급락 / 번아웃
3. **공감 전환** — "여러분은 충분히 열심히 했습니다" + BEFORE/AFTER 비교
4. **해결책 제시** — 완전 자동화 소개 + 4단계 순환 그리드 + CTA + 6단계 더보기
5. **실제 사용자 후기** — 카카오톡 캡처 스타일 3개
6. **추천 대상** — 키워드 클라우드 배경 + 떠다니는 6개 태그 카드
7. **안전성 안내** — 다크 배경 + 3카드(미니 UI) + 환불 보장 배너
8. **가격 & 플랜** — 3카드 (무료/1개월/풀패키지) + 주의사항
9. **FAQ** — 아코디언 6문항
10. **주문 폼** — 다크 배경 2컬럼
11. **사업자 푸터** — 사업자 정보 + 법적 페이지 링크 3개 + 카카오톡 문의
12. **스티키 CTA** — 하단 고정

---

## 7. JS 파일별 기능 매핑

### config.js
| 항목 | 설명 |
|---|---|
| `APP_CONFIG.supabase.url` | Supabase 프로젝트 URL |
| `APP_CONFIG.supabase.anonKey` | Supabase anon public key |
| `APP_CONFIG.plans` | 플랜 매핑 (free→free_trial, 1month→monthly, full→full_package) |
| `APP_CONFIG.kakao.channelUrl` | 카카오 오픈채팅 URL |

### main.js
| 함수/기능 | 설명 |
|---|---|
| `scrollToForm(planValue)` | 가격 카드 → 폼 스크롤 + 플랜 자동 선택 |
| `updateRadioStyles()` | 선택 라디오 초록 하이라이트 |
| `closeModal()` | 결제 안내 모달 닫기 |
| `showToast(msg, type)` | 성공/에러 토스트 (4초) |
| scroll 이벤트 | 스티키 CTA 표시/숨김 |
| `animateCountUp(el)` | 수치 카운트업 애니메이션 |
| painObserver | `.pain-card` 스크롤 fade-in |
| card1Observer | STEP 1 메시지 버블 순차 등장 (700ms) |
| card3Observer | STEP 3 번아웃 알림 등장 (1.5초 후) |
| `typeNextChar()` | 히어로 AI 댓글 타이핑 (3개 순환) |

### form.js
| 함수/기능 | 설명 |
|---|---|
| Supabase client 초기화 | config.js의 url/anonKey로 createClient |
| `getUserIP()` | ipify API로 사용자 공인 IP 조회 |
| `submitForm(e)` | create_order RPC 호출 + 토스트/모달 처리 |
| 에러 처리 | RAISE EXCEPTION 한글 메시지를 토스트에 그대로 표시 |

---

## 8. 보안 설계

| 항목 | 구현 |
|---|---|
| anon key 노출 | OK (프론트엔드 공개, RPC만 호출 가능) |
| service_role key | Edge Function Secrets + Supabase Vault에만 저장 |
| orders 테이블 직접 접근 | RLS 차단 (정책 없음), create_order RPC만 허용 |
| kakao_tokens 테이블 | RLS 차단, service_role만 접근 |
| licenses 테이블 | 기존 RLS 그대로 유지 |
| create_license 호출 | anon GRANT 없음, service_role만 가능 |
| DB Trigger → Edge Function | Authorization: Bearer + Vault에서 읽은 service_role_key |
| 무료체험 악용 방지 | 이메일 중복 체크 (create_order RPC 내부) |
| From 헤더 | RFC 5322 MIME encoded-word (비-ASCII 인코딩) |
| .gitignore | supabase/.env, .temp/, .branches/, node_modules/, .DS_Store, .vscode/, .idea/ |

---

## 9. 남은 작업

### Phase 8 (나머지): GitHub Pages + 도메인
- [ ] GitHub Pages 활성화
- [ ] 카페24 CNAME 레코드 추가 (blog → jsrb0.github.io)
- [ ] 커스텀 도메인 설정 + HTTPS

### Phase 9: 실전 테스트
- [ ] 전체 플로우 E2E 테스트
- [ ] 모바일 반응형 확인

### ⚠️ 보안 정리 (필수)
- [ ] 카카오/텔레그램 키 재발급 + Edge Function Secrets 교체
- [ ] Git 히스토리 키 노출 여부 점검

---

## 10. 디자인 결정 히스토리

| 변경 | 이전 | 이후 | 이유 |
|------|------|------|------|
| 파일 구조 | 단일 index.html 1724줄 | CSS/JS 분리 (4파일) | 유지보수성 |
| Supabase 호출 | `from('orders').insert(...)` | `rpc('create_order', {...})` | RLS 호환, 보안 |
| status 문자열 | '입금대기' | '결제대기'/'처리중'/'발송완료'/'발급실패'/'이메일실패'/'데이터오류' | 정확한 상태 표현 |
| Supabase 프로젝트 | lsykymwqzlypnichhids | egwmkpplnzypkbedasrs | 보안 강화 전환 |
| service_role_key 관리 | app.settings (DB config) | Supabase Vault | 보안 강화 |
| 이메일 브랜드 | BlogAutoFriends | 블로그 자동화 솔루션 | 브랜드 통일 |
| 이메일 디자인 | 단순 흰 배경 | 베이지 배경 + 흰 카드 + 그라디언트 CTA | 랜딩페이지 톤 맞춤 |
| 이메일 From | 평문 한글 | RFC 5322 MIME encoded-word | 스팸 판정 방지 |
| 인증키 이메일 | 기본 안내 | 경고 박스(카운트다운+환불) + 혜택 표 + 가이드 버튼 | 전환율 + 법적 보호 |
| 푸터 | 단순 저작권 표시 | 사업자 정보 + 법적 페이지 3개 링크 | 전자상거래법 준수 |
| 히어로 헤드라인 | "AI가 하루 100명과 소통..." | "상위 1% 블로거들의 비밀 무기" | 임팩트 강화 |
